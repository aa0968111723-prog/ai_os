/**
 * WB-01 stub — full resource drawer lands in WB-05 (prompt library, results, traces).
 * Kept as a no-op export so the workbench shell can mount a stable slot.
 */
export function CreationResourceDrawer(_props: {
  projectId: string;
  open?: boolean;
  onClose?: () => void;
}) {
  return null;
}
