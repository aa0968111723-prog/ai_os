# MCP Recommended Tool Order

whoami → get_project_context → list_knowledge / query public fuel table → list_scenes → list_assets → generate → set_scene_visual → post_message

Always load Style Bible and identity rules before generating.
