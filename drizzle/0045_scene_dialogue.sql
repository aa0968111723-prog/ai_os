-- 逐鏡對白：一鏡的說話序列，「@師父：坐吧。」這樣一行一句，旁白用 @旁白 標記，
-- 兩者可以交錯而仍分得出來（結構由 shared/sceneSpeech.ts 讀取時導出、永不落庫）。
--
-- 為什麼存原文而不是結構化 jsonb：applyScript 的保守契約（省略＝維持原值、永不刪除）
-- 建立在字串比對上。結構化儲存會讓「解析器沒完全看懂的那一行」在來回之後被正規化、
-- 被重排——使用者自己打的字被系統改掉。解析失敗的代價必須是「這句沒標到人」，
-- 不能是「這句少了三個字」。
--
-- nullable，既有分鏡不受影響；既有 voiceover 一個位元組都不動，讀取時視為排在最前面的旁白。
ALTER TABLE "scenes" ADD COLUMN IF NOT EXISTS "dialogue" text;
