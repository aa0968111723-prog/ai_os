# 修復計畫 2026-08-04

> 狀態盤點日：2026-08-04  ·  目標 base：`claude/healing-migration-ai-os-erewp2`  ·  執行方式：本機終端機逐步落地

本文件只放**計畫與 checklist**，不直接改產品程式。程式修復請依 Phase 在本機 commit 後推 PR。

---

## 0. 最新修復狀況（盤點）

### 0.1 base 分支現況（tip ≈ `530356d`）

| 檔案 / 模組 | tip 狀態 | 嚴重度 |
|---|---|---|
| `client/src/components/GenerationList.tsx` | `PLACEHOLDER_USE_ARTIFACT_FINAL`（2 行 stub） | **P0 部署阻塞** |
| `client/src/pages/DatabasesPage.tsx` | 曾被縮成 ~4KB 空殼（見 #390） | **P0** |
| `client/src/pages/PlannerPage.tsx` | 曾被縮成約 ⅓（見 #390） | **P0** |
| `server/services/mcp.ts` | 曾被砍成不完整協定層（catalog 與 handler 脫節） | **P0 CI / MCP** |
| migration bridge `LEGACY_ADOPTION_PENDING_TAGS` | 只到 0030，pending 已到 0034 | **P0 CI** |
| 靈感頻道 `publishFromSource` 後端 | 已存在（6 種 sourceType） | OK |
| 靈感頻道 UI 發布入口 | 僅 `PromptLibrary` 有；GenerationList / 角色卡 / 素材無 | **P1 產品缺口** |
| `toggleLike` | 無表、無 mutation | P2 |

### 0.2 相關 open PR

| PR | 標題 | 狀態 | 備註 |
|---|---|---|---|
| **[#390](https://github.com/aa0968111723-prog/ai_os/pull/390)** | fix(repair): 還原 MCP、三頁、migration bridge→0034 | **draft / dirty** | 最完整的大修；**不含** GenerationList「發布」按鈕 |
| **[#388](https://github.com/aa0968111723-prog/ai_os/pull/388)** | hotfix: restore GenerationList + publish | open / dirty | 分支內容仍是 placeholder 字串，**未真正推入完整檔** |
| #369 | hotfix: restore handleMcp export | open | 可能已被後續修覆；合併前確認是否仍需要 |
| #316 / #245 / #226 | docs / audit / push deeplink | open | 非本輪阻塞 |

### 0.3 資安項（深度檢查後）— **多數已在碼內修掉，勿重開**

| 原滲透項 | 現況 |
|---|---|
| XFF 偽造繞過登入限流 | ✅ 已改 `req.ip` + `trust proxy = 1` |
| SSRF 不做 DNS | ✅ `assertPublicHostOrError` + 全 A/AAAA + 每跳 redirect |
| 工作流繞過成本核准 | ✅ TD-02：走 `executeGenerationCommand` |
| tRPC stack 外洩 | ✅ `errorFormatter` 一律剝 stack |
| 封存專案仍可寫 | ✅ `projectState.assertProjectAllows` |
| schedule 成員改他人行程 | ✅ `scheduleWriteDenied` |
| **仍開** | RLS 全無（P0 路線圖）、DNS rebinding TOCTOU（殘留，建議出口代理） |

---

## 1. 執行原則

1. **先救 CI / 還原被清空檔**，再補產品功能。
2. **一個 Phase 一個 PR**（或一個 commit 群），方便 review 與 rollback。
3. 大檔還原**禁止**用「placeholder 字串」當內容推上；本機確認 `wc -l` 與關鍵符號後再 push。
4. base 是 `claude/healing-migration-ai-os-erewp2`（目前 default branch）。

---

## 2. Phase 清單（終端機可執行）

### Phase A — 合併 / 落地 #390（P0 大修） ⏱️ 建議最先

**目標**：MCP 協定層、DatabasesPage、PlannerPage、GenerationList 本體、migration bridge 0031–0034、測試替身、audit wording。

```bash
# 1) 同步 base
git fetch origin
git checkout claude/healing-migration-ai-os-erewp2
git pull --ff-only

# 2) 檢查 #390 分支能否乾淨 rebase
git checkout claude/broken-v1nnue   # #390 head
git rebase origin/claude/healing-migration-ai-os-erewp2
# 若 conflict：優先保留 #390 對 mcp.ts / 三頁 / migration 的完整內容

# 3) 本機驗證（最低門檻）
npm ci
npm run typecheck
npm test
npm run test:client:coverage
npm run check:ui-primitives
sh scripts/ci-migration-test.sh   # 需本機 Postgres

# 4) 確認關鍵檔不是 placeholder
wc -l client/src/components/GenerationList.tsx   # 期望 ≥ 700
wc -l client/src/pages/DatabasesPage.tsx         # 期望 ≥ 1500
wc -l client/src/pages/PlannerPage.tsx           # 期望明顯大於 24KB 級縮水版
grep -n "PLACEHOLDER" client/src/components/GenerationList.tsx && echo "FAIL" || echo "OK"

# 5) 推回並把 #390 標為 ready（或新開 PR 取代 dirty 狀態）
git push --force-with-lease origin HEAD:claude/broken-v1nnue
# GitHub：#390 Ready for review → merge（建議 squash）
```

**完成定義**

- [ ] `typecheck` 0 錯
- [ ] unit + client coverage 全綠
- [ ] migration test 綠
- [ ] tip 上 GenerationList / DatabasesPage / PlannerPage **皆非 placeholder**
- [ ] MCP e2e（或至少 catalog ↔ TOOLS 對齊測試）綠

---

### Phase B — GenerationList「發布」按鈕（P1 產品主路徑）

**目標**：在 **已還原** 的 GenerationList 上加 `publishFromSource`（約 20–30 行）。

> ⚠️ #388 目前分支內容仍是 placeholder，**不要直接 merge #388**。在 Phase A 合併後的 tip 上重做小 PR。

```bash
git checkout claude/healing-migration-ai-os-erewp2
git pull --ff-only
git checkout -b feat/generationlist-publish-button

# 編輯 client/src/components/GenerationList.tsx
# 參考實作要點（對齊 PromptLibrary 既有寫法）：
# 1. state:
#    const [publishState, setPublishState] = useState<{ id: string; ok: boolean } | null>(null);
# 2. mutation:
#    const publish = trpc.community.publishFromSource.useMutation({
#      onSuccess: (_d, vars) => { setPublishState({ id: vars.sourceId, ok: true }); utils.community.invalidate(); },
#      onError: (_e, vars) => setPublishState({ id: vars.sourceId, ok: false }),
#    });
# 3. 在 canEdit && g.status === "done" 的操作欄加按鈕：
#    onClick={() => publish.mutate({ sourceType: "generation", sourceId: g.id })}
#    文案：發布 / 已發布 ✓ / 發布失敗；disabled={publish.isPending}

# 測試替身：若有 GenerationList 相關 test，補 community.publishFromSource mock
# PromptLibrary 測試曾缺此替身（#390 已補模式可抄）

npm run typecheck
npm run test:client:coverage -- GenerationList 2>/dev/null || true

git add client/src/components/GenerationList.tsx
git commit -m "feat(community): GenerationList 完成列一鍵發布到靈感頻道"
git push -u origin HEAD
# 開 PR → base: claude/healing-migration-ai-os-erewp2
```

**手動測**

- [ ] 生成完成列出現「發布」
- [ ] 點擊後靈感頻道 feed 可見
- [ ] 「我的」可下架
- [ ] Remix / 一鍵再用仍正常

**完成後可關閉** #388（標 superseded by 本 PR）。

---

### Phase C — 其他卡發布入口（P1 可選）

後端 `sourceType` 已支援；前端入口缺：

| 元件 | sourceType |
|---|---|
| CharacterCards | `character` |
| Scene preset 卡 | `scene_preset` |
| Prop 卡 | `prop` |
| AssetLibrary | `asset` |

實作模式同 Phase B（mutation + 按鈕 + invalidate）。可合成一支 `feat/community-card-publish` PR。

---

### Phase D — Community toggleLike（P2）

需新 migration：

- 表 `community_likes (post_id, user_id, created_at)` PK `(post_id, user_id)`
- mutation `toggleLike`：upsert / delete + 同步 `community_posts.likeCount`
- UI：CommunityPage 愛心可點

單獨 PR，勿與 Phase A/B 混。

---

### Phase E — 官方技術債 P0（營運，非本輪產品）

見 `docs/技術債清償路線圖.md`：

1. PostgreSQL RLS 第一階段（私訊、資料庫、專案／素材／生成、點數）
2. 正式監控告警（5xx、p95、runner、Volume）
3. 備份還原演練證據（PG + Volume 同一時間邊界）

資安殘留：正式環境建議出口代理白名單，收斂 SSRF DNS rebinding TOCTOU。

---

## 3. 建議合併順序

```
Phase A  (#390 落地)     ──►  tip 可建、CI 綠
    │
    ▼
Phase B  (發布按鈕)      ──►  靈感頻道生成主路徑完整
    │
    ├─► Phase C (其他卡)     可並行
    └─► Phase D (like)       可並行
    │
    ▼
Phase E  (RLS / 監控 / 備份)  準正式營運前
```

**不要做的事**

- 不要 merge 仍含 `PLACEHOLDER_*` 字串的 #388
- 不要在未 rebase 的 dirty draft 上「以為已修」
- 不要重開已修的 XFF / SSRF DNS / cost gate 工單

---

## 4. 一鍵健康檢查（任意 Phase 後可跑）

```bash
npm run typecheck \
  && npm test \
  && npm run test:client:coverage \
  && npm run check:ui-primitives \
  && npm run check:boundaries \
  && npm run audit:high

# 檔案完整性
for f in \
  client/src/components/GenerationList.tsx \
  client/src/pages/DatabasesPage.tsx \
  client/src/pages/PlannerPage.tsx \
  server/services/mcp.ts
do
  if grep -q 'PLACEHOLDER' "$f" 2>/dev/null; then echo "BROKEN $f"; else echo "ok $f ($(wc -l < "$f") lines)"; fi
done
```

---

## 5. 變更紀錄

| 日期 | 說明 |
|---|---|
| 2026-08-04 | 初版：盤點 tip placeholder、#388/#390 狀態、資安已修項、Phase A–E 終端機步驟 |
