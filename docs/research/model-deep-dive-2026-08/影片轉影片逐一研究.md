# 影片轉影片（V2V）逐一研究 #1–#8（2026-08）

> 來源：fal 官方 schema / llms.txt / OpenAPI 對照站內 `shared/models.ts` video-to-video 條目。  
> 本檔與 I2V / T2V 研究卡同格式，聚焦參數名、預設值、價格與站內落差。

---

## #1 Topaz Video Upscale (`fal-ai/topaz/upscale/video`)

| 欄位 | 內容 |
|------|------|
| **官方 Endpoint** | `fal-ai/topaz/upscale/video` |
| **Category** | video-to-video |
| **站內 id / verified** | `fal-ai/topaz/upscale/video` / true |
| **Needs** | video |
| **站內 points / cost** | 7 點 / `$0.01–0.08/秒(依解析度);按秒計費,點數為 6 秒基準` |
| **官方 Required** | `video_url` |
| **官方 Optional** | `model` (ModelEnum, default `"Proteus"`)<br>`upscale_factor` (float 1–4, default 2)<br>`target_fps` (int 16–60，設了才啟用插幀)<br>`H264_output` (bool, default false → 輸出 H265) |
| **model 選項** | Proteus（通用最佳）／Artemis HQ·MQ·LQ（去噪+銳化）／Nyx·Nyx Fast·Nyx XL·Nyx HF（專職去噪）／Gaia HQ·CG·2（渲染/動畫）／Starlight Precise 1/2/2.5·HQ·Mini·Sharp·Fast 1/2（生成式擴散） |
| **站內 input()** | `(_p, _f, s) => ({ video_url: s })` |
| **落差** | **P1**：完全未暴露 `model` / `upscale_factor` / `target_fps`。目前永遠吃 Proteus + 預設 2x。建議至少固定 `model: "Proteus", upscale_factor: 2`，或產品決策暴露進階選項。 |
| **架構筆記** | Topaz 自家非擴散模型家族；插幀可選 Apollo 系。業界標準升頻，低清舊素材救星。 |
| **建議** | 保持 verified:true；文件標「預設 Proteus 2x」；後續可加 UI 選擇 model。 |

---

## #2 Sync Lipsync v2 Pro (`fal-ai/sync-lipsync/v2/pro`)

| 欄位 | 內容 |
|------|------|
| **官方 Endpoint** | `fal-ai/sync-lipsync/v2/pro` |
| **Category** | video-to-video |
| **站內 id / verified** | `fal-ai/sync-lipsync/v2/pro` / true |
| **Needs** | video |
| **站內 points / cost** | 155 點 / `$5/分;按影片長度計費,點數以 1 分鐘短片估,較長影片實際費用更高` |
| **官方 Required** | `video_url` + `audio_url` |
| **官方 Optional** | `sync_mode` (SyncModeEnum, default `"cut_off"`)<br>選項：`cut_off` / `loop` / `bounce` / `silence` / `remap` |
| **站內 input()** | `(p, _f, s) => ({ video_url: s, audio_url: p.trim() })` |
| **落差** | **P1**：用 prompt 欄塞 `audio_url`，功能可行但 UX 差（使用者需把音訊網址貼在提示詞）。`sync_mode` 未暴露，永遠 cut_off。sourceHint 已提示「提示詞欄貼音訊網址」，但仍易誤解。 |
| **價格備註** | Pro $5/分；標準 v2 約 $3/分。長片費用遠高於扁平估點。 |
| **架構筆記** | Sync Labs 最新一代對嘴，保留牙齒與臉部細節；支援多種 duration mismatch 處理模式。 |
| **建議** | 短期維持現狀 + 強化 sourceHint；中期產品決策獨立 audio source 欄位或明確 UI 說明。 |

---

## #3 RIFE (`fal-ai/rife/video`)

| 欄位 | 內容 |
|------|------|
| **官方 Endpoint** | `fal-ai/rife/video` |
| **Category** | video-to-video |
| **站內 id / verified** | `fal-ai/rife/video` / true（recommended） |
| **Needs** | video |
| **站內 points / cost** | 2 點 / `$0.0013/運算秒(極低);點數為 6 秒基準` |
| **官方 Required** | `video_url` |
| **官方 Optional** | `num_frames` (int 1–4, default 1)<br>`use_scene_detection` (bool, default false)<br>`use_calculated_fps` (bool, default true)<br>`fps` (int 1–60, 僅當 use_calculated_fps=false)<br>`loop` (bool, default false) |
| **站內 input()** | `(_p, _f, s) => ({ video_url: s })` |
| **落差** | **P2**：基本正確。可選加 `num_frames: 1` 明確化；進階參數（scene detection / fps）可維持預設。 |
| **架構筆記** | 開源即時補幀（RIFE），24→48/60fps 或慢動作。站內 recommended，最適合 AI 生成影片卡頓修飾與老影片流暢感重建。 |
| **建議** | 維持現狀；文件標「預設補 1 幀」。 |

---

## #4 SeedVR2 Video Upscale (`fal-ai/seedvr/upscale/video`)

| 欄位 | 內容 |
|------|------|
| **官方 Endpoint** | `fal-ai/seedvr/upscale/video` |
| **Category** | video-to-video |
| **站內 id / verified** | `fal-ai/seedvr/upscale/video` / false |
| **Needs** | video |
| **站內 points / cost** | 12 點 / `$0.001/百萬像素(寬×高×幀數);點數以 6 秒 1080p 30fps 估` |
| **官方 Required** | `video_url` |
| **官方 Optional** | `upscale_mode` ("factor"\|"target", default "factor")<br>`upscale_factor` (1–10, default 2)<br>`target_resolution` (720p/1080p/1440p/2160p)<br>`noise_scale`、`output_format`、`output_quality` 等 |
| **站內 input()** | `(_p, _f, s) => ({ video_url: s })` |
| **落差** | **P1**：未暴露 upscale_mode / factor / target_resolution。價格按「總像素×幀數」計，站內估點需注意長片/高解析會遠超扁平值。 |
| **架構筆記** | 字節 SeedVR2 擴散式放大，時序一致性佳，單價極低，適合整批歷史影音。 |
| **建議** | 文件標「按總像素計費，長片費用高」；可預設 `upscale_mode: "factor", upscale_factor: 2`。 |

---

## #5 BEN2 影片去背 (`fal-ai/ben/v2/video`)

| 欄位 | 內容 |
|------|------|
| **官方 Endpoint** | `fal-ai/ben/v2/video` |
| **Category** | video-to-video |
| **站內 id / verified** | `fal-ai/ben/v2/video` / false |
| **Needs** | video |
| **站內 points / cost** | 5 點 / `$0.001/百萬像素(幾乎零成本);點數以 6 秒 720p 30fps 估` |
| **官方 Required** | `video_url` |
| **官方 Optional** | `background_color` (RGB array 或 transparent)<br>`output_format` ("mp4"\|"webm", default mp4；webm 支援 alpha) |
| **站內 input()** | `(_p, _f, s) => ({ video_url: s })` |
| **落差** | **P2**：基本正確。透明背景需 webm；站內未暴露 format。 |
| **架構筆記** | Background Erase Network v2，時序平滑去背，單價極低的日常主力。 |
| **建議** | 維持；文件標「預設 mp4，透明請用 webm」。 |

---

## #6 LatentSync (`fal-ai/latentsync`)

| 欄位 | 內容 |
|------|------|
| **官方 Endpoint** | `fal-ai/latentsync` |
| **Category** | video-to-video |
| **站內 id / verified** | `fal-ai/latentsync` / false |
| **Needs** | video |
| **站內 points / cost** | 6 點 / `$0.20/≤40秒,之後 $0.005/秒;長片實際費用高於扣點` |
| **官方 Required** | `video_url` + `audio_url` |
| **站內 input()** | `(p, _f, s) => ({ video_url: s, audio_url: p.trim() })` |
| **落差** | **P1**：同 Sync，用 prompt 欄塞 audio_url。極低成本開源對嘴（字節）。 |
| **架構筆記** | 音訊條件式 latent diffusion 對嘴，真人與動畫皆可。 |
| **建議** | 同 Sync 的 UX 建議；適合大量草稿對嘴。 |

---

## #7 MuseTalk (`fal-ai/musetalk`)

| 欄位 | 內容 |
|------|------|
| **官方 Endpoint** | `fal-ai/musetalk` |
| **Category** | video-to-video |
| **站內 id / verified** | `fal-ai/musetalk` / false |
| **Needs** | video |
| **站內 points / cost** | 5 點 / `≈$0.10–0.20/次(推估)` |
| **官方 Required** | `video_url` + `audio_url` |
| **站內 input()** | `(p, _f, s) => ({ video_url: s, audio_url: p.trim() })` |
| **落差** | **P1**：同 Sync 的 audio 處理。 |
| **架構筆記** | 只改嘴部區域、不重繪全臉，失真風險低（保守型）。適合「師父影像不能失真」的場合。 |
| **建議** | 同 Sync；文件標「保守型、只改嘴」。 |

---

## #8 Sync Lipsync v3 (`fal-ai/sync-lipsync/v3`)

| 欄位 | 內容 |
|------|------|
| **官方 Endpoint** | `fal-ai/sync-lipsync/v3` |
| **Category** | video-to-video |
| **站內 id / verified** | `fal-ai/sync-lipsync/v3` / false |
| **Needs** | video |
| **站內 points / cost** | 155 點 / `約$5+/分(略高於 v2,以 fal 現場為準)` |
| **官方 Required** | `video_url` + `audio_url`（推估同 v2 Pro） |
| **站內 input()** | `(p, _f, s) => ({ video_url: s, audio_url: p.trim() })` |
| **落差** | **P1**：同 v2 Pro 的 audio UX + 價格略高。 |
| **架構筆記** | Sync 最新一代，對嘴自然度天花板。 |
| **建議** | 同 v2 Pro。 |

---

## 後續 V2V 候選（待補完整卡）

| 模型 | 站內 id | 主要落差方向 |
|------|---------|--------------|
| Bria Video Background Removal | `bria/video/background-removal` (+v3 / realtime) | endpoint 命名空間、價格 |
| Lucy Edit / Restyle | `decart/lucy-edit` / `decart/lucy-restyle` | 文字改影片 / 長片風格轉換 |
| Wan VACE Outpainting | `fal-ai/wan-vace-14b/outpainting` | 外擴畫幅 |
| Sync Lipsync 1.9 / v2 | 多個 | 價格分層 |
| VEED 系列 | veed/* | 商用去背 / 字幕 / 對嘴 |
| BiRefNet v2 Video | `fal-ai/birefnet/v2/video` | 開源去背 |
| Auto Caption / Subtitle | fal-ai/auto-caption 等 | 字幕燒錄 |
| Bria Video Eraser | `fal-ai/bria/video/eraser` | 去物件 |

---

## 與站內落差文件關係

本檔詳細 schema 對照 → 摘要可執行工單寫入 `站內落差與修復建議.md` 的 V2V 段落。

*I2V P0（Kling start_image_url + allowlist）本地已完成，remote models.ts 仍需人工覆蓋。*
