# 站內落差與修復建議（2026-08 模型底層研究）

> 來源：文生圖 #1–#30 對照 fal 官方 schema。  
> 本 PR 已落地 **P0 allowlist 修正**；其餘列為後續工單。

## 本 PR 已修（程式）

| 項 | 變更 | 檔案 |
|----|------|------|
| AuraFlow 誤收 negative | 自 `NEGATIVE_PROMPT_SUPPORTED` **移除** `fal-ai/aura-flow`（官方 schema 無此欄，送了恐 422） | `shared/models.ts` |
| Qwen 中文字卡吃禁忌 | **加入** `qwen-image-2`、`qwen-image-2/pro`、`qwen-image-max` | 同上 |
| FLUX LoRA 可負向 | **加入** `fal-ai/flux-lora`（fal 頁列有 negative_prompt） | 同上 |
| 單測 | AuraFlow=false；Qwen 三端點與 flux-lora=true | `shared/models.test.ts` |

禁忌詞注入路徑（無需改）：`generationCore` 在 `supportsNegativePrompt(model)` 為真時寫入 `falInput.negative_prompt`。

## P0 待處理（端點存活）

| 模型 | 問題 | 建議 |
|------|------|------|
| Seedream 5.0 Pro `…/seedream/v5/text-to-image` | fal 頁 **404** | `probe-fal-endpoints` 確認後改 slug 或降級挑選器／標 deprecated |
| Ideogram v4 `fal-ai/ideogram/v4` | llms.txt **404** | 同上；並確認商業授權 |
| Hunyuan Image 3.0 | 路徑曾 404 | blog 有上架；probe 後更新 id |

## P1 參數對齊

| 模型 | 問題 | 建議 |
|------|------|------|
| Recraft V3／V4.1 | 官方 `size`，站內 `image_size` | 改 `input()` 欄位名；可選暴露 `style: vector_illustration` |
| Ideogram v3 | 站內 `aspect_ratio` vs 官方 `image_size` | OpenAPI 核對後統一 |
| FLUX.2 flex | 可調 steps／guidance 未暴露 | 產品決策：進階面板或固定高品質預設 |
| 多數模型 | `input()` 偏瘦 | 維持簡化 OK；文件標「進階參數吃預設」 |

## P2 計價／文案

| 模型 | 問題 |
|------|------|
| Nano Banana 2 | 目錄 $0.06–0.16 vs 官方基準 ~$0.08 |
| Ideogram v3 | 檔位價 vs 預設 BALANCED |
| Seedream 5 | cost 已標「待 fal 計價確認」— 誠實，待端點復活後更新 |

## 與既有文件關係

- 不取代 `docs/fal生態研究.md`（全生態廣度）  
- 不取代 `docs/模型指南研究補遺.md`（情境／風格配方）  
- 本系列補 **底層機制 + 逐模型 schema 對照 + allowlist 可執行修正**

## 建議後續 PR

1. 跑 `FAL_KEY=… npx tsx scripts/probe-fal-endpoints.ts --yes` → 產出連通報告，一次清 404  
2. Recraft／Ideogram `input()` 參數名修正 + 單測  
3. 圖生圖 66 條同格式研究卡  
4. 影片家族時序 DiT 底層專章

