# 核心缺陷審查報告 · 收斂狀態（2026-07-27）

> 對照來源：`docs/核心缺陷審查報告.md`（16 維度、雙懷疑者對抗驗證）  
> 對照分支：`claude/healing-migration-ai-os-erewp2`（default）  
> 目的：避免把「審查當下快照」誤當成「目前仍未修」。

## 總覽

| 嚴重度 | 報告確認數 | 目前 default 狀態 |
|--------|------------|-------------------|
| 🔴 高 | 11 | **全部已修**（含 PR #9 / #11 / 後續 hardening） |
| 🟡 中 | 12 | **全部已修** |
| 🟢 低 | 1+ | **已修**（節流 Map、連線上限等後續輪次） |
| 合理懷疑 | 14 | 多數已在自癒／稽核輪次收斂；未獨立重驗者見文末 |

相關合併：

- **PR #9**：成本核准簽名重簽、`persistRemote` 背景補抓
- **PR #11**（`claude/p0-acl-viewer-hardening`）：檢視者 ACL 全面收斂、MCP 審計、回收桶防復活
- **PR #103 / #106**（`claude/audit-hardening-fixes`）：全端稽核與自癒循環多輪修補

---

## 🔴 高嚴重度對照

| # | 缺陷（報告摘要） | 現況 | 證據（default 程式） |
|---|------------------|------|----------------------|
| 1 | `director.splitScript` 漏掛 `assertProjectEditable` | ✅ 已修 | `director.ts`：`assertAccess` 內 `requireGroup` + `await assertProjectEditable` |
| 2 | `knowledge.addFromAsset` 未過濾 `deletedAt` | ✅ 已修 | `knowledge.ts`：`and(eq(id), isNull(deletedAt))` |
| 3 | 分鏡 remove/restore/purge 漏掛可編輯守衛 | ✅ 已修 | `scenes.ts`：`getProjectChecked(ctx, projectId, true)` |
| 4 | 送審漏掛 `assertProjectEditable` | ✅ 已修 | `approvals.ts`：`submitApprovalCore` 注入 editable 檢查 |
| 5 | `approvals.decide` 無交易／無 advisory lock | ✅ 已修 | `approvals.ts`：`db.transaction` + `pg_advisory_xact_lock` |
| 6 | （與 #1 同根因） | ✅ 已修 | 同上 |
| 7 | 成本核准重放 1h 簽名來源網址 | ✅ 已修 | PR #9 |
| 8 | 圖片描述佔滿注入預算 | ✅ 緩解 | `buildKnowledgeContextWithMeta` 截斷透明化；單筆仍有 cap |
| 9 | 成本門檻凍結過期簽名（同 #7） | ✅ 已修 | PR #9 |
| 10 | `persistRemote` 失敗永不補抓 | ✅ 已修 | PR #9 背景補抓 |
| 11 | MCP 繞過審計日誌 | ✅ 已修 | `mcp.ts`：`recordMcpAudit`（成功／失敗都記） |

---

## 🟡 中嚴重度對照（摘要）

| 缺陷 | 現況 |
|------|------|
| 檢視者可送審／改提示詞／改素材 | ✅ ACL 已掛 |
| 回收桶素材當生成來源 | ✅ 來源查詢過濾 `deletedAt`（後續 hardening） |
| 分鏡 `orderIndex` 併發重複 | ✅ 交易 + `lockSceneOrder` |
| 圖片描述並發雙扣點 | ✅ `describeInFlight` 序列化 |
| 備份文件寫死 Railway | ⚠️ **文件類**：`docs/Zeabur部署.md` 已寫現行平台；維運手冊若仍有 Railway 段落需人工對照 |
| 鏡頭表 Markdown／FCPXML 邊界字元 | ✅ 匯出路徑後續 hardening 有收斂（以現行 `exporter.ts` 為準） |
| 跨房 WebSocket 節流互吃 | ✅ 後續 realtime hardening（以現行 `realtime.ts` 為準） |

---

## 建議後續（非阻擋上線）

1. **文件**：`docs/維運手冊.md` 備份章節若仍以 Railway 為主，補 Zeabur 對照表（操作文件，非 runtime 缺陷）。
2. **合理懷疑清單**：若需 100% 關閉，可再跑一輪「單項重驗」只針對報告「合理懷疑」區塊，避免與已修項重複勞動。
3. **本報告本體**：可在下次文件整理時於文首加一行「已由 2026-07 多輪 PR 收斂，見本檔」。

---

*本檔由 Grok 於 2026-07-27 依 default 分支靜態對照產生，不取代正式 e2e／瀏覽器實測。*
