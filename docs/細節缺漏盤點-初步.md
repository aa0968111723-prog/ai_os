# 細節缺漏盤點（初步・逐功能完整性）

> 日期：2026-07-17　範圍：逐功能端到端完整性稽核（26 功能中 22 個完成）。
> 註：此為大規模盤點的**部分成果**——容器重啟＋工作階段用量上限中斷了逐檔（123 檔×2）與跨切面掃描，
> 僅「逐功能完整性」階段完成並保留於此（219 項）。完整盤點待用量重置後續跑。

**總計 219 項**：HIGH×12　MEDIUM×80　LOW×102　NIT×25

| 嚴重度 | 功能 | 缺漏 | 觸發情境 | 建議 |
|---|---|---|---|---|
| HIGH | 專案生命週期（projects：建立/封存/ | 封存專案在 Web(tRPC) 仍完全可寫，僅 MCP 有守衛——跨介面不一致 | 組長封存某專案 → 成員用瀏覽器打開 /p/<id>（封存後仍能經直連或最近開啟進入）→ 在生成台送出生成 → 後端照常 reserveQuota 扣點並送 fal，成功寫入 ge | 在 tRPC 側把『封存＝寫入凍結』做成與 MCP 一致的共用守衛（例如在 assertProjectEditable 或另一 assert |
| HIGH | 專案生命週期（projects：建立/封存/ | 沒有任何列出/篩選已封存專案的 UI，封存後幾乎無法還原 | 使用者封存專案後離開該頁 → 作業台看不到它 → 沒有『已封存』檢視可列出 → 若最近開啟名單已被其他 12 個專案擠掉、又沒存網址，該封存專案實質上再也找不到、無法還原（內容並未 | 在 Launchpad 增加『顯示已封存 / 已封存篩選』開關（傳 includeArchived:true）並在卡片上提供還原入口；或提供 |
| HIGH | 登入/註冊/邀請/改密碼/重設（auth） | acceptInvite 的 try/catch 把所有錯誤（含 DB 內部錯）一律包成 BAD_REQUEST 並外洩原始訊息 | 兩人同時點同一邀請連結（或 DB 短暫抖動）→ 第二個請求在 insert users 撞 email unique → 使用者看到一句英文 SQL 約束錯誤，且此內部錯誤不會被  | 只捕捉 acceptInvite 內部明確 throw 的 Error（用 sentinel/自訂 class 或訊息前綴判斷）轉 BAD_ |
| HIGH | 工作流串鏈（workflows） | 送出前放置的佔位 generationId 讓陳屍回收器 case(b) 失效，run 可能永久卡 running | reserveQuota 因連線池耗盡/序列化衝突持續丟例外時，某步驟送出反覆失敗；run 每 4 秒重試但永不終局，30 分鐘後 sweepZombies 也無法回收（佔位 ge | sweepZombies case(a) 收到 advanceGeneration NOT_FOUND 時，應把該 step 視同『無生成』 |
| HIGH | AI 代理生命週期（agents：規劃/核准 | MCP 入口未驗 uuid 格式，非法 id 觸發 DB 例外變成 500（與 router 的 .uuid() 守衛不一致） | 外部 AI 透過 MCP 呼叫 approve_agent({runId:'abc'}) 或 plan_agent({projectId:'x'})：後端丟出 500『AI 代理暫 | 在 agentCore 各 core 入口用 z.string().uuid().safeParse 檢查 projectId/runId， |
| HIGH | 審批三態機（approvals：送審/通過/ | 分鏡/審批查詢失敗被靜默吞掉，錯誤偽裝成空狀態、組長裁決鈕直接消失 | 若 approvals.listByProject（line 477）請求失敗（後端 500／網路斷線／權限變動），approvals.data 為 undefined → pen | 分別處理 scenes.error / approvals.error（role=alert 顯示可重試訊息），並把空狀態與錯誤狀態區分開； |
| HIGH | 分鏡（scenes：排序/就地生成/時長） | generateInto 缺模型類型守衛，與 generateVoiceover 不對稱 | 使用者（或 stale 前端／直呼 tRPC）對 generateInto 傳一個無 needs 的 audio 模型 id：submitGenerationCore 照樣扣點送出 | 比照 generateVoiceover，在 generateInto 內以 getModel(input.modelId) 檢查 kind |
| HIGH | 提示詞庫（prompts） | 生成 prompt 無 max 上限，但 prompts.save 限 2000 字 → 超長咒語靜默不入庫 | 使用者貼上 >2000 字的提示詞成功生成 → 後端 prompts.save 被 Zod 擋下丟錯 → 前端無 onError 吞掉 → 使用者以為「打過的咒語已入庫」，但提示詞 | generation.submit 的 prompt 加上與 save 一致的 .max(2000)（或提高 save 上限至一致），並給  |
| HIGH | 自訂資料庫（databases：四層範圍/欄 | refreshFile 缺 importUrl 的「登入頁 HTML」守衛，來源轉私有時靜默覆寫好內容並刪原檔 | 使用者以 Google 文件公開連結匯入一份文件（正常抽到內文），之後該文件被設為私有；使用者按「重新整理」→ 內容被替換成 Google 登入頁的雜訊文字，且原始已抽好的資料/原 | 在 refreshFile 對 normalized.kind.startsWith('google-')（非 google-drive）且 |
| HIGH | 資料庫文件匯入（databaseFiles： | refreshFile 缺 Google 登入頁/HTML 守衛，會靜默覆蓋成登入頁文字（資料遺失） | 使用者匯入一份公開 Google Doc（有正文）→ 擁有者把共用改為私有 → 使用者按「重新整理」→ textContent 被覆寫成 Google 登入頁文字，原正文遺失且無任 | refreshFile 在 normalized.kind.startsWith('google-') 且 kind!=='google-d |
| HIGH | 資料庫文件匯入（databaseFiles： | refreshFile 對抓不到文字的網頁靜默清空內容（與 importUrl 不一致） | 匯入的網頁來源改版為 SPA/純 JS 渲染 → 使用者按重新整理 → htmlToText 回空 → textContent 被清為 '' 且回 {ok:true}，使用者以為更 | refreshFile 抽出文字為空時拋錯並保留原內容，與 importUrl 同一句錯誤訊息。 |
| HIGH | 筆記排程＋.ics（notes/schedu | schedule.add 的 ownerId 未校驗屬同組（與 mentions/projectId 守衛不一致，會洩漏外組成員名字） | 呼叫 schedule.add 帶入任一存在於別組的 user UUID 當 ownerId → 該行程被存下 → 組內任何人打開排程清單即看到外組使用者的真實姓名（跨組個資洩漏） | 在 addScheduleItemCore 對 ownerId 做同組成員校驗（查 groupMembers），非成員擋 BAD_REQUE |
| MEDIUM | 專案生命週期（projects：建立/封存/ | projects.list 無 LIMIT/分頁，無界查詢 | 一個長期使用、累積數千個專案的組，開作業台會把全部專案一次撈出並傳到瀏覽器，payload 與 DB 掃描隨專案數線性膨脹。 | 加上合理 limit 與游標/分頁（cursor 或 offset），前端『顯示更多』改為真正向後端分頁。 |
| MEDIUM | 專案生命週期（projects：建立/封存/ | 功能不完整：專案只有封存，沒有任何刪除/永久刪除端點 | 使用者誤建大量測試/垃圾專案（或含敏感內容的專案）→ 只能封存隱藏、無法刪除 → DB 永久堆積，且封存專案仍被 list(includeArchived) 與各種跨專案彙總查詢掃 | 若刻意只做軟刪除，應在 UI 明講『不可刪除、只可封存』並修正誤導文案；否則補上 owner/組長可用的刪除（軟刪或含級聯清理的 purge |
| MEDIUM | 專案生命週期（projects：建立/封存/ | 專案標題缺長度上限與伺服器端 trim，且與 renameAsset 驗證不一致 | 以 tRPC/REST 直接呼叫 projects.create 傳 title='   '（三個空白）或數萬字標題 → 伺服器接受並入庫 → 作業台卡片封面取 title.cha | create 標題改為 z.string().trim().min(1).max(80)（與 renameAsset 對齊），前端輸入框加  |
| MEDIUM | 登入/註冊/邀請/改密碼/重設（auth） | acceptInvite 併發兌換同一 token 無原子性保護，靠 email unique 兜底且回應醜陋 | 使用者連點兩下「完成加入」（前端 canSubmit 有 isPending 擋，但網路重送/多分頁可繞過）→ 兩個 acceptInvite 並發 → 一個成功登入、另一個吐 D | 改用條件式 UPDATE invites SET acceptedAt=now() WHERE token=? AND acceptedAt |
| MEDIUM | 登入/註冊/邀請/改密碼/重設（auth） | 強制改密碼期間若 changePassword 觸發限流，使用者被完全鎖死於死路 | 組員拿到管理員給的臨時密碼，抄錯連打 5 次 → 被限流 15 分鐘 → 強制對話框卡住、背景 inert、無任何可操作項，體感等同帳號壞掉。 | 強制模式的限流訊息要明確引導（例：請 X 分鐘後再試，或登出重來）；或對 mustChangePassword 情境放寬/分離限流門檻。 |
| MEDIUM | 登入/註冊/邀請/改密碼/重設（auth） | login 與 acceptInvite 為 publicProcedure，完全不進審計；失敗登入無任何留存 | 事後要調查某帳號是何時被兌換建立、或有無異常登入嘗試 → audit_log 完全查不到，因為這些是 public procedure 未納入審計。 | 在 login 成功/失敗、acceptInvite 成功時各補一筆審計（可用輕量 recordAudit 或 console audit） |
| MEDIUM | 登入/註冊/邀請/改密碼/重設（auth） | clientIp 直接信任 X-Forwarded-For 首段，IP 限流可被偽造標頭繞過 | 撞庫工具對同一 email 之外的大量 email 逐一嘗試，並每次帶隨機 X-Forwarded-For → IP 維度限流永不觸發，只受各 email 各自 5 次限制拖慢，帳 | 在反代層設定可信 proxy 並只取可信跳數的 IP；或在應用層驗證 XFF 來源，無可信來源時退回 socket.remoteAddres |
| MEDIUM | 登入/註冊/邀請/改密碼/重設（auth） | acceptInvite 後端 name 僅 z.string().min(1) 再 trim，全空白名字可通過建帳號 | 直接對 auth.acceptInvite 送 name='   ' → 建立一個顯示名稱為空字串的帳號，之後在各處 UI 呈現為空白成員。 | 後端改用 z.string().trim().min(1)（zod 先 trim 再驗），與前端一致。 |
| MEDIUM | 工作流串鏈（workflows） | 『同人同專案單一工作流』僅靠 check-then-insert，無 DB 層唯一約束，可被併發繞過 | 使用者快速雙擊執行、或程式化連打兩次 start，兩請求交錯執行：都 SELECT 到無 active、都成功 INSERT，同專案同人同時跑兩條工作流，各自並行扣點。 | 以部分唯一索引（unique on (project_id,user_id) where status='running'）或在交易內 SE |
| MEDIUM | 工作流串鏈（workflows） | 多實例部署下背景執行器會重複推進同一 run（inflight 僅限單一進程） | 部署為 2 個以上 web 實例時，任一活躍 run 的當前步驟會被兩實例同時送出，產生重複生成與重複扣款；退點/收尾也各走一份。 | 背景推進改用 DB 層鎖（SELECT ... FOR UPDATE SKIP LOCKED 取 run）或以單一 leader/專用 wo |
| MEDIUM | 工作流串鏈（workflows） | start 的 prompt 伺服器驗證與前端不一致：min(1) 不 trim、無 max，允許純空白想法 | 以純空白或超長字串直接呼叫 workflows.start：純空白通過驗證後跑出一串空提示詞生成並照常扣點；超長 prompt 從 start 無阻擋進入，但同樣內容走助手則被 2 | start 改用 `z.string().trim().min(1).max(2000)`（或與 run_workflow 對齊的共用 sc |
| MEDIUM | 工作流串鏈（workflows） | 前端對兩個核心查詢無載入/錯誤/空狀態 UI，查詢失敗靜默無提示 | 網路抖動或後端錯誤導致 workflows 或 listByProject 查詢失敗：畫面靜默呈現空下拉/空清單，使用者無法分辨『真的沒有』還是『載入失敗』，也沒有重試入口。 | 為兩查詢加入載入中骨架、錯誤橫幅（含重試）與空狀態文案。 |
| MEDIUM | 生成台（generation：送出/輪詢/退 | 失敗重試遺失角色/場景錨點與分鏡綁定 | 使用者帶 3 角色定裝＋2 場景設定生成失敗→按重試→產出的是無錨點版本，跨鏡一致性喪失；若原本是綁分鏡的 generateInto 失敗，重試出的成品因缺 sceneId 不回填 | 重試改以原 generationId 為藍本，或在 generation 列持久化 characterIds/scenePresetIds/ |
| MEDIUM | 生成台（generation：送出/輪詢/退 | 分鏡就地生成 generateInto 缺模型 kind 守衛（與 generateVoiceover 不一致） | 對 generateInto 傳 text 模型→扣點但 advanceGeneration 因 kind==text 不入素材庫、不回填，分鏡格永遠空白且點數已扣；傳 audio | generateInto 比照 generateVoiceover 檢查 model.kind 屬視覺類（image/video），否則 B |
| MEDIUM | 生成台（generation：送出/輪詢/退 | 自動回填以 sceneRole 判斷、手動 setVisual 以 asset.kind 判斷—落點不一致 | 以 audio 模型走 generateInto（sceneRole=visual/null）自動回填→進 assetId（主畫面、破圖）；同一 audio 成品若手動『設為此鏡現 | 兩處統一判斷依據（建議都以 asset.kind 為準）。 |
| MEDIUM | 生成台（generation：送出/輪詢/退 | 組員+審核門檻+分鏡就地生成：awaiting_approval 無任何 UI 回饋 | 組員設有審核門檻，於分鏡格按『生成』被門檻攔下回 awaiting_approval→SceneList 無生成中指示、按鈕復原、無提示→組員以為沒送出而反覆點擊，堆出多筆待核列全 | pendingGenStatus 納入 awaiting_approval 並顯示『待核准』，或 generateInto/generate |
| MEDIUM | 生成台（generation：送出/輪詢/退 | router 版陳屍清掃無 limit 且阻塞列表回應 | 某專案累積大量 queued/running 停滯列（如批次工作流中斷）→一打開生成列表就被整批 ledger 查詢與退點寫入拖慢，且無上限。 | router 版 sweep 加 .limit(BATCH)，與 runner 同口徑；或完全交給背景 runner，列表不阻塞於退點。 |
| MEDIUM | AI 代理生命週期（agents：規劃/核准 | listByProject 回傳所有 awaiting_approval+running（無上限、無分頁），且未核准計畫永不過期 | 使用者反覆規劃卻不核准也不放棄（規劃免費、每分鐘可 4 次），數十上百筆 awaiting_approval run 累積在同一專案，listByProject 每次全撈、前端全渲 | active 段加合理上限與分頁；或對 awaiting_approval 加過期機制（例如 N 天自動 discarded），避免無界累積 |
| MEDIUM | AI 代理生命週期（agents：規劃/核准 | AgentCard 未處理列表查詢的載入中/錯誤/空狀態（查詢失敗靜默無提示） | listByProject 因網路/伺服器錯誤失敗時，使用者看到的就是一張只有輸入框、下方空無一物的卡片，不知道是沒有計畫還是載入失敗。 | 加上 runs.isLoading 的載入態、runs.error 的 role=alert 錯誤態，以及 data 為空時的引導文案。 |
| MEDIUM | AI 代理生命週期（agents：規劃/核准 | 核准前未做點數餘額預檢，估點僅顯示不比對餘額 | 使用者只剩 2 點卻核准估點 20 點的計畫：前面免費步驟照跑，跑到第一個 generate 步驟才因點數不足失敗，計畫半成品、體驗差；核准當下沒有任何『點數可能不足』提示。 | 核准畫面比對 estPoints 與目前可用點數，不足時明確警示（不必硬擋，但要揭示）。 |
| MEDIUM | AI 代理生命週期（agents：規劃/核准 | 缺代理生命週期的測試覆蓋（併發/冪等/停止競態等關鍵邏輯未測） | 未來重構 saveRun 或核准鎖時，停止競態被完成覆寫、或重複扣點等回歸無測試可攔。 | 補 agentCore（核准併發、狀態機轉移、權限）與 agentRunner（停止競態、冪等恢復、重試上限）的單元/整合測試。 |
| MEDIUM | AI 代理生命週期（agents：規劃/核准 | 執行期不重查發起人專案層 ACL：規劃後被降為檢視者仍會續跑寫入 | A 規劃並核准一份 12 步計畫，執行到第 3 步時被組長改為該專案檢視者；後續 9 步仍以 A 身分建分鏡、生成、送審、寫資料庫，違反『檢視者不能寫入』的規則。 | 至少在每步送出前重查發起人對該專案的可編輯性（比照 status 重查的空窗處理），權限被收回即收攏成 failed。 |
| MEDIUM | 點數與額度（quota/points：估/扣 | 週/日用量為「全域跨組」但週/日額度上限卻是「逐組解析」——多組使用者互相吃額度 | 使用者同屬 A、B 兩組、各設週額度 300。他在 B 組花了 250 點後切到 A 組專案生成，A 組週額度雖『理應』還有 300，但 usedThisWeek 回 250（含  | 若週/日屬個人整體速率限制，quota 解析就不該逐組（會誤導成組級額度）；若屬組級速率限制，usedThisWeek/usedToday  |
| MEDIUM | 點數與額度（quota/points：估/扣 | 成本審核門檻只守『直接單筆生成 submit』，AI 代理／工作流／語音／@助手全繞過 | 組長把門檻設 5 點想攔住組員的高價生成，組員改用『AI 代理』或『工作流』發動同樣模型的大量生成，估點遠超門檻卻完全不需核准即扣點送出，成本審核形同虛設。 | 至少把門檻檢查下沉到 reserveQuota 或所有『成本入口』共用的守門層，或在 agent/workflow 啟動時對 member  |
| MEDIUM | 審批三態機（approvals：送審/通過/ | 送審/裁決 mutation 為全表共用單一實例，任一格操作會 disable 其它所有格的按鈕 | 組長點「通過」第 1 鏡時，decide.isPending=true 使第 2..N 鏡的通過/退回鈕全部 disable，直到該筆完成；一批分鏡無法並行裁決，長清單逐格審批被強 | 比照 update/generate 把 submit/decide 下放為每格自持的 mutation，或以 variables（appr |
| MEDIUM | 審批三態機（approvals：送審/通過/ | 退回理由未顯示在分鏡卡，提交人看到「需修改」卻不知道要改什麼 | 組長退回並填理由後，提交組員在分鏡列表看到紅色「需修改」badge，但卡片上沒有任何理由文字，必須切換到留言區翻找系統訊息才知道被退回的原因，修改動線斷裂。 | 分鏡卡在 needs_work 狀態時就地顯示 pendingOf/最新審批列的 reason（approvals 已回傳該欄），或在卡片提 |
| MEDIUM | 審批三態機（approvals：送審/通過/ | scene 狀態與 approval 狀態來自兩條獨立輪詢 query，存在裁決鈕短暫消失的競態窗 | 別的使用者送審某分鏡後，本端 scenes query 先刷新（分鏡變 pending 顯示待審 pill），approvals query 尚未刷新（pendingOf 回 un | 以單一 query 同時回分鏡與其最新審批（或在 scenes 回傳內嵌 latestApproval），避免兩來源時間差造成鈕時有時無。 |
| MEDIUM | 站內留言＋@助手＋即時同步（messages | 釘選留言超出最近 50 則視窗後從釘選列消失 | 組長釘一則決議 → 之後對話累積超過 50 則 → 釘選列突然空白，決議「被洗掉」，正是釘選要防止的情況。 | 在 list 內對 pinned=true 另跑一次不受 limit 50 限制的查詢（或 union），確保釘選留言恆在。 |
| MEDIUM | 站內留言＋@助手＋即時同步（messages | 留言無分頁／載入更多，超過 50 則的歷史永久不可見 | 專案累積 200 則留言 → 使用者想找兩週前的討論或引用卡 → 無任何 UI 可載入，資料等同遺失。 | 加 cursor（createdAt/id）分頁與「載入更早」按鈕；或至少提供搜尋。 |
| MEDIUM | 站內留言＋@助手＋即時同步（messages | @提及以子字串比對，名字互為前綴時誤標並誤通知 | 成員含「阿明」與「阿明師兄」；@阿明師兄 → 阿明也收到桌面通知與未讀提及徽章。 | send() 改成與 renderBody 相同的長名優先、以邊界/token 比對；或前端只送 mention 下拉實際點選的 userI |
| MEDIUM | 站內留言＋@助手＋即時同步（messages | 錄音中卸載元件未停止 MediaRecorder，麥克風軌未釋放（資源洩漏） | 按下錄音 → 未按停止就切到別頁 → 麥克風持續開啟，隱私與資源雙洩漏。 | 加 useEffect 回傳 cleanup：unmount 時 recorderRef.current?.stop() 並停止所有 tra |
| MEDIUM | 站內留言＋@助手＋即時同步（messages | message_reactions 無唯一約束，併發重按灌大表情計數 | 使用者快速雙擊 🙏 → 兩筆插入 → 顯示「🙏 2」其實只有一人；再按只收回一筆。 | 加 (messageId,userId,emoji) 唯一約束並改用 upsert/onConflict；或在應用層序列化。schema.t |
| MEDIUM | 知識庫＋版本＋回收桶（knowledge） | 永久刪除知識未清理其版本快照，textVersions 永久孤兒累積 | 使用者對一筆改過多次（累積 20 版快照）的逐字稿按「永久刪除」→ knowledge 列被刪、但 20 筆 text_versions 全文留在 DB 永不被清；長期大量 pur | purge 時一併 db.delete(textVersions).where(and(eq(kind,'knowledge'), eq(r |
| MEDIUM | 知識庫＋版本＋回收桶（knowledge） | title 於 zod min(1) 後才 trim，空白標題會被存成空字串 | API 直接以 title="   "（純空白）呼叫 knowledge.add → 通過驗證、DB 標題存成空字串 → KnowledgeBase 清單與 RecycleBin  | 改用 z.string().trim().min(1).max(120)（或 refine 檢查 trim 後非空），add/update  |
| MEDIUM | 知識庫＋版本＋回收桶（knowledge） | list / listVersions / listDeleted 為了摘要卻 SELECT 全文，記憶體與 DB 傳輸無界 | 一個累積數十份 4 萬字逐字稿的專案開啟知識庫 → list 一次把上百萬字全文載入 server 記憶體僅為產生 120 字摘要，浪費頻寬與記憶體、回應變慢。 | 在 SQL 層取長度與截斷（如 length(content)、substr/left(content,120)）而非 select 全文再 |
| MEDIUM | 知識庫＋版本＋回收桶（knowledge） | describeImageAsset：扣點成功但最終入庫 insert 失敗時未退點 | 視覺模型回傳成功、扣點完成，但當下 DB 短暫故障使 insert 拋錯 → 使用者付了點數、知識庫沒有新描述、錯誤是原始 DB 例外而非退點訊息。 | 將 insert 納入 try/catch，失敗時 refund 並回可讀錯誤；或整段扣點+入庫包一致的補償邏輯。 |
| MEDIUM | 角色定裝（characters） | list 查詢錯誤被偽裝成「還沒有角色」空狀態 | 後端暫時掛掉或斷網時開專案 → 使用者已建的角色卡全部消失、畫面說「還沒有角色」，誤導使用者以為資料遺失，甚至去重建，且沒有重試按鈕。 | 比照 GenerationList 加 list.isError 分支：顯示錯誤文案與 list.refetch() 重試鈕，不落 empt |
| MEDIUM | 角色定裝（characters） | 後端 name/appearance 先驗長度再 trim → 全空白字串被存成空字串 | 直接呼叫 characters.add 傳 name="   "（3 個空白）、appearance="  " → zod min(1) 以長度 3 通過 → trim 後存入空字 | 改用 z.string().trim().min(1) 讓驗證作用在 trim 後的值（zod 的 .trim() 會先修剪再驗長度），ad |
| MEDIUM | 角色定裝（characters） | 失敗重試漏帶 characterIds／scenePresetIds，重生成缺角色定裝注入 | 使用者勾了 2 個角色定裝生成失敗 → 按「以相同設定重試」→ 重生成不含任何角色錨點，輸出人物走樣，與原本設定不同，且文案宣稱「相同設定」造成誤導，白扣一次點。 | 在 generations 落存 characterIds/scenePresetIds，重試時回填；或至少在重試前提示「重試不會帶入原角色 |
| MEDIUM | 角色定裝（characters） | 角色勾選數無上限，超過後端 max(6) 才在送出時靜默 zod 失敗 | 勾選 7 個角色 → 確認面板顯示「帶入 7 個角色定裝」→ 按確認生成 → 後端 zod 回「Array must contain at most 6 element(s)」，僅 | toggleChar 勾到第 7 個時擋下並提示「最多帶入 6 個角色」，或在卡片區顯示已選/上限計數，與後端 max(6) 對齊。 |
| MEDIUM | 角色定裝（characters） | buildCharacterAnchor 靜默丟棄已刪除／無效的 characterId | A 勾選角色送出的同時 B 刪掉該角色（或 localStorage 殘留已刪 id 在清理 effect 前送出）→ 生成照常扣點執行，但角色錨點靜默少注入，輸出缺該角色一致性， | buildCharacterAnchor 或 submit 在 rows.length < 傳入 id 數時，至少記錄/回報「有 N 個角色 |
| MEDIUM | 分鏡（scenes：排序/就地生成/時長） | scenes.reorder 為不可達死碼，且重排邏輯有序號漏洞 | reorder 目前無入口＝功能半成品/死碼。若日後接上拖曳而只送部分 id：被略過的分鏡 orderIndex 與新序號碰撞，listByProject 依 orderIndex | 若確定不做拖曳，移除 reorder 這段未使用匯出；若要保留，改為在交易內對『全部未刪分鏡』重編序號（缺漏 id 追加在後），並與其他插入 |
| MEDIUM | 分鏡（scenes：排序/就地生成/時長） | 配音詞長度驗證前後不一致（addDraft 500 vs update 2000） | 透過 addDraft 建立草稿時配音詞被限制 500 字內，但同一格改用 update 就地編輯可存到 2000 字；使用者從草稿補詞與事後編輯得到不同上限，且 2000 字配音 | 統一兩處 voiceover 上限（擇一並與前端 InlineEdit、TTS 成本上限一致）。 |
| MEDIUM | 場景設定（scenePresets） | 檢視者(viewer)未套用唯讀：場景卡的新增/刪除/設參考圖按鈕仍可點，操作才被後端擋 | 以 viewer 身分開專案 → 點『新增場景設定』填完按『建立場景』→ 後端回 FORBIDDEN，畫面底部跳出 add.error 錯誤訊息。與同頁 KnowledgeBase | 為 ScenePresetCards 增加 readOnly 參數（比照 KnowledgeBase），ProjectPage 傳 read |
| MEDIUM | 場景設定（scenePresets） | list 查詢失敗無錯誤狀態，靜默降級成空狀態『還沒有場景』 | 後端暫時 500 或斷線時，已有數張場景卡的專案會顯示『還沒有場景』，使用者誤以為資料遺失並可能重複建立，實際只是載入失敗。應顯示錯誤與重試，而非空狀態。 | 新增 list.isError 分支顯示錯誤訊息與重試按鈕（可比照專案其他 query 的錯誤處理），空狀態僅在確定 data 為空陣列時才 |
| MEDIUM | 場景設定（scenePresets） | 前端未限制場景選取數量，與後端 scenePresetIds.max(4) 不一致，逾限直到送出才報原始 Zod 錯 | 勾選第 5 個以上場景 → 前端無提示照常可勾 → 按『確認生成』時後端 Zod 拒絕整筆生成，拋出未在地化的 array max 驗證訊息，使用者不知是哪裡超限。characte | 在 toggleScene 達上限時阻擋新增並提示（例如『最多帶入 4 個場景』），或在 ScenePresetCards 對未勾且已達上限 |
| MEDIUM | 場景設定（scenePresets） | knowledge 上下文注入未截斷 scene preset 欄位，與 characters 不一致且數量無界，可能撐爆 LLM 預算 | 專案累積數十張、每張 palette+lighting 近千字的場景卡時，45 行撈全部 preset（無 limit）並在 61 行完整注入 cardBlock，且此段標榜『不被 | 比照 characters 對 palette/lighting 做 slice（例如各 160/120），並對注入的 preset 筆數設 |
| MEDIUM | 提示詞庫（prompts） | PromptLibrary 把載入中與查詢錯誤都渲染成整塊消失，list.error 從不顯示 | list 查詢因網路/權限/伺服器錯誤失敗時，提示詞庫整塊靜默消失，使用者看不到任何「載入失敗」提示，也分不清是「沒有咒語」還是「載入出錯」。載入中同樣沒有骨架/載入態。 | 分開處理 list.isLoading（顯示載入態或不顯示）、list.error（顯示錯誤訊息），只有 data 確為空陣列時才 retu |
| MEDIUM | 提示詞庫（prompts） | list 硬上限 50 且前端無分頁，第 51 則之後看不到也刪不掉 | 長期使用的專案累積超過 50 則咒語後，排在 50 名之後的（尤其 useCount 低的舊咒語）永遠不會出現在清單，使用者無法檢視、複製或刪除它們，形成無法清理的堆積；資料庫也持 | 加 cursor/offset 分頁與「載入更多」，或提供搜尋；至少對舊/低使用咒語提供清理入口。 |
| MEDIUM | 提示詞庫（prompts） | awaiting_approval 狀態也被當成「成功生成」提前入庫 | 達門檻送核准的提示詞當下還沒真的生成，卻立刻入庫並顯示為「成功生成的咒語」。若之後被組長駁回/從未生成，這則從未成功的提示詞仍長駐在「打過的咒語」清單，與文案「成功生成的提示詞會自 | 只在 data.status 表示實際已生成（非 awaiting_approval）時才 savePrompt，或改在生成真正完成的事件點 |
| MEDIUM | 自訂資料庫（databases：四層範圍/欄 | 刪列權限：前端對所有可寫成員顯示刪除鈕，後端只允許建立者或管理者，成員刪他人列必然報錯 | memberWritable 的組資料庫，成員 A 對成員 B 建立的列按刪除→確認→跳出「只有這一列的建立者或資料庫管理者可以刪除」。刪除鈕本不該出現。 | canDelete 改為 `canManage \|\| row.createdBy===myId`（需把目前使用者 id 傳進 GridR |
| MEDIUM | 自訂資料庫（databases：四層範圍/欄 | list / listRows / listFiles 查詢無載入中與錯誤狀態，查詢失敗畫面靜默空白 | 網路抖動或後端 500 導致 list/listRows/listFiles 失敗時，使用者看到的是空清單或永久「…」，誤以為沒有資料，而非「載入失敗，請重試」。 | 為三個 query 補 isLoading 骨架與 isError 提示（role=alert），與 preview 的錯誤呈現一致。 |
| MEDIUM | 自訂資料庫（databases：四層範圍/欄 | importCsv 逐列 N+1：每列都跑一次 COUNT(*) 與 updatedAt UPDATE，5000 列達上萬次查詢 | 使用者貼上 3000～5000 列 CSV 匯入，請求長時間佔用連線、可能逼近逾時；並非邏輯錯誤但體感極慢且壓 DB。 | 批次匯入路徑改為：先查一次現有列數做上限判斷，批次驗證後以單次（或分批）多列 insert，最後只更新一次 updatedAt。 |
| MEDIUM | 自訂資料庫（databases：四層範圍/欄 | linkedToProject 一次載入多庫全部列到記憶體再於 JS 過濾，無單次查詢上限 | 多個組/團隊各有接近上限的器材表、任務表且都含 project 欄位；開啟任一專案頁觸發 linkedToProject 時，伺服器一次把這些庫全部列讀進記憶體，記憶體與延遲飆高。 | 改用 SQL 端過濾：對每個 project 欄位 key 以 jsonb 條件（data->>key = projectId）在 DB 篩 |
| MEDIUM | 資料庫文件匯入（databaseFiles： | Google Drive 大檔/病毒掃描攔截頁被當成網頁內容抽字 | 匯入一個 >100MB 或設為需登入的 Google Drive 檔連結 → 伺服器抓到 confirm/login HTML → 存成「AI 可讀」的攔截頁文字，AI 讀到錯誤內 | google-drive kind 拿到 text/html 時也視為失敗（提示改用公開直連或直接上傳），或處理 confirm token |
| MEDIUM | 資料庫文件匯入（databaseFiles： | 資料庫軟刪除不清 dataFiles 的 Volume 原檔，磁碟永久佔用 | 建立含大量大檔的資料庫 → 刪除資料庫 → 檔案不計配額也永不刪除，反覆操作會逐步耗盡 Volume 磁碟。 | 軟刪表時（或後續 purge 排程）刪除其 dataFiles 的 storagePath 實體檔，或提供孤兒檔回收工作。 |
| MEDIUM | 資料庫文件匯入（databaseFiles： | 配額檢查與寫入非原子，併發上傳可超額（TOCTOU） | 使用者剩 100MB 配額，同時併發上傳 3 個 80MB 檔 → 三者都讀到「還夠」→ 全部寫入，實際用量超額 140MB。 | 以資料庫交易＋鎖（或寫入後複核並回滾）強制配額原子性，或以樂觀重試複查。 |
| MEDIUM | 資料庫文件匯入（databaseFiles： | listFiles 與 list_database_files 硬上限 200、無分頁 | 長期累積 250 份逐字稿的資料庫 → 最舊的 50 份在 UI 與 MCP 清單完全消失，AI 引用時遺漏。 | 加 offset/游標分頁，或至少回傳總數與截斷旗標讓前端提示。 |
| MEDIUM | 筆記排程＋.ics（notes/schedu | .ics 內容行未做 RFC5545 折行（line folding），長標題/備註超 75 octet 可能被嚴格解析器拒收或截斷 | 建立一筆標題或備註很長（例如整段會議說明）的行程 → 匯出 .ics → 匯入嚴格的 iCalendar 解析器（部分 Apple/Exchange 匯入路徑）時該長行被拒或被截， | 對每條 content line 依 UTF-8 octet 數 75 折行，續行以單一空白起頭；補一個長字串測試。 |
| MEDIUM | 筆記排程＋.ics（notes/schedu | .ics/個資匯出端點的 401/403/404/500 會被瀏覽器當成 .ics 檔案下載，錯誤靜默 | session 過期後點「匯出 .ics」→ 下載到一個內容為 {"error":"請先登入"} 卻叫做 組排程.ics 的檔案，匯入日曆失敗且畫面完全沒有錯誤提示，使用者不知發生 | 改用 fetch 檢查 res.ok 後再觸發 blob 下載，非 2xx 時顯示錯誤；或端點在錯誤時仍給可辨識訊息並在前端攔截。 |
| MEDIUM | 筆記排程＋.ics（notes/schedu | 筆記版本快照只寫不讀：snapshotNote 持續存 20 版但 UI 無任何檢視/還原入口 | 使用者把一份 4 萬字會議紀錄改壞並儲存 → 舊版其實已存在 text_versions → 但前端無任何「還原上一版」按鈕，一般使用者救不回來，只能請工程師進 DB。半成品功能。 | 補筆記版本列表＋還原 UI，或若確定 v1 不做，移除誤導性的「供救急」承諾並在文案說明僅供後台救援。 |
| MEDIUM | 筆記排程＋.ics（notes/schedu | 清單/匯出多處硬上限（300/200/500）無截斷提示也無分頁，超量資料靜默遺失 | 活躍組累積 >300 筆未來行程或 >200 則筆記 → 清單只顯示前段，其餘完全消失且無任何提示；.ics 匯出 >500 筆時個人日曆缺事件，使用者以為只有這些。 | 至少在達上限時回傳 hasMore 旗標並在 UI 顯示，長期補分頁；.ics 匯出的 500 上限應提示或提高。 |
| MEDIUM | AI 導演建議（director） | 真模式 LLM 失敗退回時，前端仍標示「測試模式建議」誤導使用者 | 正式環境（非 mock）使用者按「給我 3 個分鏡 idea」，NIM 逾時或回傳非 JSON → 後端回 mock:true+fallback:true → 卡片同時顯示「AI  | 以 fallback 旗標區分：mock && !fallback 才顯示「測試模式建議」；fallback 時只顯示 fallback 提 |
| MEDIUM | AI 導演建議（director） | fallback 罐頭建議寫死佛教/禪修文案，且與 usedKnowledge 提示互相矛盾 | 專案已加入大量開示/腳本（usedKnowledge=true），真模式 NIM 429 限流 → 回 fallback 罐頭建議；卡片顯示綠色勾「已讀取專案知識庫的素材」，但三則 | fallback 時前端不要顯示「已讀取知識庫」的正向提示（或後端在 fallback 回傳 usedKnowledge:false）；並讓 |
| MEDIUM | 專案助手（assistant） | SSE 串流中途斷線會退回 tRPC 重跑整條問答，重複消耗速率額度與 NIM 呼叫 | 使用者問一題→串流吐出「正在查素材庫…」等 step 後連線被 proxy 切斷→前端 handled=false→自動退回 tRPC 再問一次。單一問題吃掉 6 次/分額度中的  | 串流一旦成功收到 open 就視為已接手（handled=true），斷線改為顯示錯誤而非退回 tRPC；或在 done 缺席時只在確認 s |
| MEDIUM | 專案助手（assistant） | SSE 問答路由未監聽 client 斷線，斷線後 LLM 工具迴圈仍跑到底 | 使用者送出問題後立刻收起助手或關掉分頁→server 端仍持續跑到 3 輪 LLM 查詢與 DB 查詢才 res.end()，運算與 NIM 免費額度被無謂燒掉；高併發下累積成明顯 | 建立 AbortController，req.on('close') 時 abort，並把 signal 傳進 callLlm/nimCom |
| MEDIUM | 專案助手（assistant） | 留言區 @助手 被限流或找不到專案時完全靜默，提問者得不到任何回饋 | 使用者在一分鐘內第 7 次 @助手（或多人共用觸發窗），這一則被靜默丟棄→對話串裡完全沒有 AI 回覆、也沒有「太頻繁請稍後」提示，提問者以為助手壞了而一直重試。與 assista | 限流時至少落一則輕量系統/助手提示（或前端顯示節流狀態）；避免使用者面對純靜默。 |
| MEDIUM | 團隊助手（teamAssistant） | 切換組後仍顯示前一組的彙總答案（跨組狀態殘留） | 組長在 A 組問「哪個案子卡住了」得到答案，切到 B 組 → 畫面仍顯示 A 組的答案、輸入框仍留著舊問句，直到重新送出才更新；使用者會把 A 組的結論誤當成 B 組的現況。 | 在 App.tsx 對 <Launchpad key={activeGroupId} …/>，或在 TeamAssistantCard 內以 |
| MEDIUM | 團隊助手（teamAssistant） | 答案區塊缺 aria-live，螢幕報讀者收不到非同步回覆 | 依賴螢幕報讀器的使用者按「詢問」，AI 回覆插入頁面後毫無語音提示，使用者不知道答案已經出現。 | 為答案容器加上 aria-live="polite"（或 role="status"），並可在 isPending 時朗讀「詢問中」。 |
| MEDIUM | 團隊助手（teamAssistant） | 非 NIM 類例外被吞掉並偽裝成正常答案回傳 | NIM 逾時被包成一般 Error、或 nimComplete 內部拋非 NimServiceError 例外 → 使用者看到「AI 彙總助手暫時沒回應，請稍後再問一次。」被當成一 | 回傳結構加上 error:true/ok:false 之類旗標讓前端能區分並顯示錯誤樣式與重試；或至少對非預期例外重新 throw（如姊妹聚 |
| MEDIUM | 回饋系統＋回饋代理（feedback/fee | emailStatus="skipped" 的回覆信永遠不會補寄，與程式碼註解宣稱的行為矛盾 | 信箱機制未設定時，使用者送出 bug 回饋 → 即時分診寫入 emailStatus="skipped"、agentReviewedAt 有值。之後管理員設定 RESEND_API | resendFailedReplies 的查詢條件改為 inArray(emailStatus, ["failed", "skipped"] |
| MEDIUM | 回饋系統＋回饋代理（feedback/fee | 即時分診與排程巡檢併發時，同一則回饋會寄出重複回覆信 | 使用者送出 bug 回饋觸發 triageReportNow，LLM 呼叫進行中（數秒）恰逢排程 tick 選取同一列：兩邊皆 sendEmail 成功 → 回報者收到兩封相同回覆 | 在寄信之前先以原子條件（UPDATE ... SET status/agentReviewedAt WHERE id=? AND agent |
| MEDIUM | 回饋系統＋回饋代理（feedback/fee | 管理頁六題滿意度彙整顯示原始 score key（context:5 cost:4），未翻成中文題目 | 管理員開回饋彙整，看到「context:5 cost:4 collab:3」而非「AI 懂不懂我們的素材：5…」，無法快速判讀哪一題分數低。 | 把 FEEDBACK_ITEMS（key→中文題目）抽到 shared/options.ts 供前後端共用，彙整列顯示中文題目。 |
| MEDIUM | 回饋系統＋回饋代理（feedback/fee | 各回饋清單皆為固定上限、無分頁，超過上限的舊資料被靜默丟棄且無「更多」提示 | 團隊長期使用後元件回饋超過 200 筆，管理員在審閱頁只會看到最新 200 筆，第 201 筆起的未處理回饋永遠不出現在清單，也沒有分頁可翻。 | 加入 cursor/offset 分頁與總數回傳；至少在達上限時於 UI 顯示「僅顯示最新 N 筆」提示。 |
| MEDIUM | 模型目錄與指南（models/catalog | syncCatalog 先 delete 全表再 insert，非交易；失敗或併發啟動會留下空目錄表 | 部署時 models.ts 不慎讓某兩筆 id 相同 → insert 整批失敗 → 目錄表被清空且無回填 → 啟動自檢報「只有 0 條」擋啟動，或（若自檢先前已過）外部代理查 m | 用 db.transaction 包住 delete+insert，使重建原子化；或改用 upsert（onConflictDoUpdate |
| MEDIUM | 模型目錄與指南（models/catalog | 工作流合計點數對「查不到的 modelId」靜默計 0，會顯示低於實扣、埋回它想修的「中途斷鏈」 | 新增工作流時把 step 寫成 `fal-ai/veo-3.1`（少個字）→ getModel 回 undefined → 該步 0 點 → 工作流卡顯示例如 27 點但實跑要 3 | 在此迴圈或啟動自檢中，對 getModel(st.modelId)===undefined 直接拋錯（fail fast），或加單元測試斷言 |
| MEDIUM | 模型目錄與指南（models/catalog | MCP 工具清單有兩份（mcp.ts TOOLS 與 shared/mcpCatalog MCP_TOOLS）卻無同步守衛/測試 | 工程師在 mcp.ts 新增讀取類工具 `list_todos` 但忘了加進 shared/mcpCatalog → 手冊看不到該工具、唯讀金鑰呼叫它被 scopeDeniedRe | 把 mcp.ts 的工具名/access 由 shared/mcpCatalog 單一來源推導（或匯出 TOOLS 後加一條測試斷言兩邊名稱 |
| MEDIUM | 模型目錄與指南（models/catalog | 4/11 類別的「推薦」預設違反「已驗證且經濟」不變式，挑選器預設選到旗艦或未驗證模型 | 使用者開啟語音轉文字面板不改選 → 預設就是 flagship 檔，成本高於文件承諾的「經濟日常主力」；開 LLM 面板 → 預設項標示「推薦 未驗證（首跑可能失敗）」，語意衝突。 | 把這 4 類的 recommended 改指向已驗證的經濟檔；並加測試：每個非 workflow 類別恰有 1 個 recommended  |
| MEDIUM | MCP 介面與金鑰（mcp/mcpToken | 封存專案守衛不完整：approve_agent／plan_agent／add_schedule_item 繞過 archived 檢查（仍會 | 外部 AI 客戶端持有某已封存專案的 projectId，呼叫 plan_agent 再 approve_agent（或 add_schedule_item）。archived 守 | 把 archived 判定前移成所有 projectId 綁定寫入工具的共同前置（或在 agentCore/scheduleCore 內對  |
| MEDIUM | MCP 介面與金鑰（mcp/mcpToken | REST v1／CSV／ICS 端點對同一把金鑰缺少暴力嘗試速率限制與失敗審計 | 攻擊者改用 GET /api/v1/databases 帶 x-api-key 猜測金鑰，可無限次嘗試而不會像 /api/mcp 一樣在 10 次失敗後被封鎖，且系統留不下任何失敗 | 抽出共用的 per-IP 失敗限流與失敗記錄，套用到 restApi 的所有金鑰認證入口，與 handleMcp 同口徑。 |
| LOW | 專案生命週期（projects：建立/封存/ | project_members 缺 UNIQUE(projectId,userId)，setProjectRole 併發可產生重複列 | 組長在兩個分頁/雙擊快速把同一成員設為檢視者 → 兩個請求交錯執行 delete/insert → project_members 出現同 (projectId,userId) 兩 | 加 unique(projectId,userId) 約束並在 insert 用 onConflictDoUpdate；或把 delete+ |
| LOW | 專案生命週期（projects：建立/封存/ | 成員離組後 project_members 覆蓋列未清理，重新加入會靜默套回舊 viewer 角色 | 成員 A 在某專案被設為檢視者 → A 被移出組 → 之後 A 又被重新加入同組 → 其在該專案的舊 viewer override 仍在，A 一進來就被靜默降為唯讀，沒有任何提示 | 移除組成員時一併刪除該使用者在該組各專案的 project_members 列；或在 listMemberRoles 顯示 override |
| LOW | 專案生命週期（projects：建立/封存/ | createSample 繞過 create 對發布平台的啟用驗證 | 某組把所有 platform 選項停用後，成員按『建立範例專案』→ createSample 仍以 youtube/16:9 建出範例；但同組用正常建立流程指定 youtube 會 | createSample 選平台時對齊 create（只取啟用中選項，無啟用選項則明確報錯或明示退回內建的取捨），保持行為一致。 |
| LOW | 登入/註冊/邀請/改密碼/重設（auth） | changePassword 未檢查新密碼是否與原密碼相同 | 管理員重設後給臨時密碼 abc12345，使用者在強制對話框把新密碼也填 abc12345 → 通過，mustChangePassword 清除，但密碼等同沒換。 | 新舊相同時擋下並提示（bcrypt.compare(new, oldHash) 為真即拒）。 |
| LOW | 登入/註冊/邀請/改密碼/重設（auth） | AdminPage CopyButton 與各儲存提示的 setTimeout 未在卸載時清除 | 管理員複製邀請連結後 2 秒內離開 /admin 或該列因 invalidate 重掛 → 舊計時器仍在，於卸載元件呼叫 setCopied/setSaved。 | 各元件加 useEffect(()=>()=>clearTimeout(timer.current),[]) 清理。 |
| LOW | 登入/註冊/邀請/改密碼/重設（auth） | acceptInvite 未驗證 invite 指向的 team/group 是否仍存在，可插入懸空成員關係 | 管理員發邀請 → 之後刪掉該組 → 受邀者兌換 → 建帳號成功但入的是幽靈組，登入後看到空狀態卻不知原因。 | 兌換時重查 team/group 是否存在，不存在則回明確錯誤（連結所屬團隊/組已不存在，請索取新邀請）。 |
| LOW | 登入/註冊/邀請/改密碼/重設（auth） | resetMemberPassword 未檢查目標帳號狀態（可對 disabled 帳號重設） | 管理員對一個已停用的離職成員按「重設密碼」→ 拿到臨時密碼交付對方，對方仍無法登入，雙方困惑。 | target.status!=='active' 時回明確提示（此帳號已停用，重設密碼無法讓其登入），或先阻擋。 |
| LOW | 工作流串鏈（workflows） | 工作流顯示點數為扁平加總，忽略 TTS 逐字估點，含語音步驟的流程顯示點數≠實扣 | 使用者對『有聲場景（經濟）』輸入很長的旁白想法：卡片顯示『約 −10 點』，但 TTS 步驟按字數實扣遠高於顯示值，帳單與預期不符。 | 對含逐字計費模型的 preset，於顯示層標註『語音步驟依字數計費，實際點數可能不同』，或以當前 prompt 長度即時估算。 |
| LOW | 工作流串鏈（workflows） | 桌面完成通知對專案內所有使用者的 run 觸發，並帶出他人想法片段 | 多人專案中，A 開著工作流卡，B 跑的工作流完成，A 的桌面跳出『工作流完成』並顯示 B 的想法片段，A 誤以為是自己的任務完成。 | 通知只針對 `r.userId === me.data?.user.id` 的 run 觸發。 |
| LOW | 工作流串鏈（workflows） | 執行紀錄終局清單固定上限 5 筆且無分頁，無法回顧更早歷史 | 一個活躍專案跑過數十條工作流後，使用者想查兩天前那條失敗 run 的原因，清單只留最近 5 筆終局，無從查起。 | 提供分頁或依需求載入更多的歷史查詢。 |
| LOW | 工作流串鏈（workflows） | 工作流執行器與 router 守衛無單元測試覆蓋，複雜併發/冪等/回收邏輯僅靠 e2e | 日後調整 settleStep 或 sweepZombies 時引入退點重複/漏退或 run 卡死，無測試即時攔截，只能靠正式環境或人工 e2e 發現。 | 針對 advanceRun/settleStep（done/failed/stopped 各路徑）、sweepZombies（case a/ |
| LOW | 生成台（generation：送出/輪詢/退 | 純空白提示詞未擋（前後端驗證不一致） | 送 prompt='     '→通過驗證、依長度扣點、生成無意義內容（TTS 會唸空白）。 | server 端對 prompt 做 trim 後再驗 min(1)，或 zod 加 .trim()/.refine 非空。 |
| LOW | 生成台（generation：送出/輪詢/退 | addFromGeneration 素材缺失時靜默建出無圖分鏡 | 成品入庫時 DB 抖動導致 asset 未寫→使用者按『加入分鏡』→成功建立一個沒有畫面的空分鏡格，看不出原因。 | asset 為空時 BAD_REQUEST 提示『成品素材尚未就緒，稍後再試』。 |
| LOW | 生成台（generation：送出/輪詢/退 | fal 回 done 但結果為空時永卡到 30 分陳屍清掃 | 供應商回 done 但無輸出網址/文字→使用者盯著『生成中…』達 30 分鐘才轉失敗退點，體感卡死。 | done 但結果為空時直接視為 failed（帶明確錯誤訊息）並走退點路徑。 |
| LOW | 生成台（generation：送出/輪詢/退 | 生成確認彈窗缺 dialog 語意與焦點管理 | 鍵盤/報讀器使用者開啟確認彈窗後焦點仍在頁面其他處，無法感知彈窗、也無法 Esc 取消。 | 加 role='dialog' aria-modal、開啟時 focus 確認鈕、Esc 綁定關閉、焦點陷阱。 |
| LOW | 生成台（generation：送出/輪詢/退 | advanceGeneration 分鏡回填不檢查分鏡是否已軟刪/存在 | 提交就地生成後、成品完成前該分鏡被刪除→成品仍寫入已軟刪分鏡的 assetId，素材綁到隱形格（還原後才出現，或永遠孤立）。 | 回填前確認 scene 未 deletedAt，或在 where 加 isNull(deletedAt)。 |
| LOW | AI 代理生命週期（agents：規劃/核准 | 前端 AgentStep 型別漏 record_to_database，後端已支援該步驟種類 | AI 代理計畫含 record_to_database 步驟時，核准畫面該步顯示 Sparkles 而非資料庫圖示；後續有人以為 kind 只有 5 種而漏處理。 | 前端 AgentStep.kind 補上 'record_to_database' 並在 KIND_ICON 加對應圖示（如 Databas |
| LOW | AI 代理生命週期（agents：規劃/核准 | 規劃限流 Map 對一次性使用者永不清理，跨進程亦無效 | 長時間運行、大量不同使用者各觸發一次規劃：hits Map 條目持續累積不釋放，屬緩慢記憶體洩漏；水平擴充時每分鐘實際可規劃次數 = 4×實例數。 | 以定時清掃或 LRU 上限清理過期 key；限流需跨實例時改用共享儲存（如 Redis）。 |
| LOW | AI 代理生命週期（agents：規劃/核准 | 操作錯誤共用單一 actionError，多筆 run 時無法對應是哪一筆失敗 | 專案有 3 筆待核准計畫，對第 2 筆核准失敗（例如『你已有一個代理在跑』），錯誤顯示在卡片頂端，使用者以為是第 1 筆的問題。 | 將 mutation 錯誤依 runId 就地顯示於對應 run 區塊。 |
| LOW | AI 代理生命週期（agents：規劃/核准 | stopAgentCore 在 CAS 落空後回傳可能為 undefined 的 current | 併發下 run 剛好被清除或狀態被他路改動導致重讀落空時，回傳 undefined 而非拋 NOT_FOUND。 | current 為空時明確拋 NOT_FOUND/PRECONDITION_FAILED，保證回傳型別。 |
| LOW | AI 代理生命週期（agents：規劃/核准 | discard/stop 前端以 canEdit 收斂，被降為檢視者的發起人無法從 UI 停止自己的執行 | 發起人被降為檢視者後想停止自己仍在跑的代理，卡片上看不到『停止後續步驟』按鈕，只能等它跑完。 | 停止/放棄按鈕的顯示條件與伺服器一致（本人或組長即可，不綁 canEdit）。 |
| LOW | 點數與額度（quota/points：估/扣 | 全域設定存檔：僅 fileQuotaGb 取整，總預算/週/日小數值會被後端 z.int() 靜默擋下 | 開發者在『週額度』欄輸入 300.5 存檔，前端送 300.5，後端 zod int 驗證失敗回原始錯誤字串，使用者看到一段技術性錯誤而非『請填整數』，且不知為何存不進去（file | 三個點數欄一併 Math.round（比照 fileQuotaGb），或前端明確提示需整數。 |
| LOW | 點數與額度（quota/points：估/扣 | awaiting_approval 生成無逾時/清掃，永久滯留 | 組員送出一筆待核生成後，唯一有權裁決的組長離職/離開該組（或長期不處理），該列永遠卡 awaiting_approval，提交者無法自行取消、也不會過期，生成紀錄堆積殭屍待核列（雖 | 提供提交者自行取消待核，或加待核逾時自動駁回（比照 30 分陳屍口徑），避免無主待核永久滯留。 |
| LOW | 點數與額度（quota/points：估/扣 | 工作流逐步扣點、無整體預留，UI 顯示『總點數夠』卻可能中途被額度擋斷 | 使用者週額度剩 100、工作流總估 100，UI 顯示『夠用』開跑；期間他在別分頁另花 30 點，工作流跑到後段某步 reserveQuota 被擋，前面已成功步驟照扣不退，留下半 | 工作流啟動時對『整包總估點』先做一次 reserveQuota 預留（或明確告知逐步扣、可能中途中斷），避免顯示夠用卻半路斷鏈。 |
| LOW | 點數與額度（quota/points：估/扣 | messageAssistant 用 isMockMode() 而非 billingBypassed() 判斷是否扣點，與其他扣點路徑不一致 | 以 MOCK_BILLING=1 跑額度守門的 e2e 時，@助手路徑不會扣點也不被額度閘擋，與生成/語音路徑行為分岔，測試覆蓋出現盲點且真實/測試口徑不一致。 | 改用 billingBypassed() 統一判斷，或在 isMockMode 分支保留與其他路徑一致的 MOCK_BILLING 扣點語意 |
| LOW | 點數與額度（quota/points：估/扣 | usedTotal() 為整表 SUM，隨每次 quota.my 徽章（60s 輪詢）與每次有總預算時的 reserve 反覆全表聚合 | 帳本累積數十萬列後，每位在線使用者每分鐘觸發一次全表聚合，且設了總預算時每筆生成守門都在全域鎖內全表掃描，尖峰時放大鎖持有時間與 DB 負載。 | 維護一個累計消耗的物化計數/彙總表，或至少對 costLedger 建立支援此聚合的索引，避免熱路徑無界全表 SUM。 |
| LOW | 點數與額度（quota/points：估/扣 | 語音逐字稿逾時即退點並標失敗，但 fal 請求未取消、可能仍完成（已計費） | wizper 轉錄實際在逾時後幾秒才完成：系統已退點並把訊息標失敗，fal 端仍計了真實費用，逐字稿結果被丟棄——真金白銀花了卻退了點、也沒拿到結果。 | 逾時後保留 requestId 交由後續輪詢/掃描收斂（成功則補回逐字稿、不退點；確定失敗才退），或呼叫 fal 取消。 |
| LOW | 點數與額度（quota/points：估/扣 | 未使用的匯出：checkQuota / deduct / effectiveWeeklyQuota 已無生產呼叫點（死碼、有分岔風險） | 日後有人誤用 checkQuota+deduct 這對（非原子、無 advisory lock）重建扣點流程，即重蹈 TOCTOU 併發雙扣；或修改守門規則時漏改這份影子邏輯造成兩 | 移除這三個未使用匯出（budgetReason 的共用測試可保留），單一守門入口只留 reserveQuota / loadQuotaCon |
| LOW | 審批三態機（approvals：送審/通過/ | 缺少職責分離：組長可對自己送審的分鏡自我核准 | 身兼 leader 的使用者送審自己的分鏡後可立即自行按「通過」，沒有任何第二人審核；若業務預期審批需他人裁決，此處被繞過。 | 若需求要求他人審核，decide 增加 submittedBy!==decidedBy 的守衛或明確標記為設計取捨。 |
| LOW | 審批三態機（approvals：送審/通過/ | approvals.listByProject 無上限/分頁，每次重送都新增一列且每 10 秒全量拉回 | 長壽專案、多次重送與多分鏡下審批列無限成長，前端每 10 秒下載並在記憶體保存全部歷史列，payload 與 find 掃描隨時間線性膨脹。 | listByProject 加 limit/分頁，或後端改回「每分鏡最新一筆」的聚合查詢供卡片使用。 |
| LOW | 審批三態機（approvals：送審/通過/ | 分鏡軟刪除後其 pending 審批成為孤兒，永不收斂且復原後可被裁決舊內容 | 有 pending 審批的分鏡被刪→審批列永久停在 pending（幽靈資料）；若之後從回收桶還原分鏡，該舊 pending 重新可被裁決，內容可能已與當初送審時不同，狀態機出現「 | 分鏡軟刪時把其 pending 審批一併標為作廢（needs_work/超時），或還原分鏡時重置其審批狀態。 |
| LOW | 審批三態機（approvals：送審/通過/ | Launchpad「待審 N」角標對無裁決權的一般組員也顯示，點進去無任何動作 | 一般組員在首頁看到專案卡「待審 3」，點進分鏡卻因非 leader 而沒有通過/退回鈕、也無說明，形成看得到卻無法處理的待辦，造成困惑。 | 「待審」角標僅對該組 leader/admin 顯示（或改文案為「送審中」對組員表達為狀態而非待辦）。 |
| LOW | 審批三態機（approvals：送審/通過/ | scenes.status 無 DB enum 約束，狀態可漂移到未涵蓋值，UI 與送審條件只認固定集合 | 若任何路徑把 scene.status 寫成非預期字串，pill 直接顯示英文原始值（line 275 fallback），且該分鏡連送審鈕都不出現（line 423 條件不匹配） | scenes.status 加 enum 約束與 approvals/流程對齊，或前端對未知狀態提供保底處理與送審入口。 |
| LOW | 審批三態機（approvals：送審/通過/ | 審批三態機最複雜的並發/版本/裁決邏輯無單元測試，僅少量 e2e 覆蓋送審權限 | 這些是最容易在重構中破壞、且錯誤後果嚴重（重複裁決／狀態被舊版覆寫／版本錯亂）的路徑，卻無迴歸網；一次改動即可在無告警下引入 lost-update 或雙裁決。 | 為 submit（版本遞增、作廢舊 pending）、decide（CAS 擋雙裁決、latest 守衛、needs_work 需理由）補單 |
| LOW | 站內留言＋@助手＋即時同步（messages | message_reads 無唯一約束，首次併發 markRead 產生重複水位列 | 同帳號兩分頁同時開專案 → 首次 markRead 併發 → message_reads 出現兩筆同 user+project。 | 加 (userId,projectId) 唯一約束並 onConflict 更新 lastReadAt。 |
| LOW | 站內留言＋@助手＋即時同步（messages | WebSocket 重連後使用者自身編輯 zone 不會重送，他人看不到其焦點 | 甲在「分鏡」區編輯 → 網路瞬斷重連 → 乙的畫面上甲的「正在這裡」外框消失且不再出現。 | onopen 或重連成功後重置 lastZoneRef=null 並依當前焦點重送一次 focus。 |
| LOW | 站內留言＋@助手＋即時同步（messages | 重連達 30 次上限後即時同步靜默終止，無使用者可見提示 | 伺服器長時間不通 → 30 次重試耗盡 → 之後所有協作者游標/在場狀態靜止，使用者以為沒人在線。 | 暴露 connection state（如 self=null 且已放棄）給 UI，顯示「即時協作已中斷，重新整理可恢復」。 |
| LOW | 站內留言＋@助手＋即時同步（messages | 桌面通知：上次已讀留言滑出視窗時 seenIdx=-1，回頭通知整批 50 則 | 使用者離開分頁久，其間湧入 >50 則 → 回來瞬間可能一次跳出多則桌面通知。 | seenIdx===-1 時視為只看最新一則或不回溯，避免整窗誤發。 |
| LOW | 站內留言＋@助手＋即時同步（messages | postVoice 只 requireGroup，未擋 viewer，與前端「editor 才能錄音」不一致 | 唯讀成員繞過 UI 直呼 messages.postVoice，用專案內既有音檔素材貼出語音留言。 | 確認語音是否允許 viewer；若不允許，postVoice 補上與上傳一致的編輯權守衛。 |
| LOW | 站內留言＋@助手＋即時同步（messages | @助手觸發限流超額時靜默丟棄，發問者毫無回饋 | 使用者一分鐘內第 7 次 @助手 → 被靜默忽略 → 使用者以為助手故障，重複再問加劇。 | 至少對觸發者落一則輕量 system/assistant 提示（可自我節流避免洗版），告知暫時過於頻繁。 |
| LOW | 知識庫＋版本＋回收桶（knowledge） | describe 節流計數在冪等去重之前，重複點擊已存在描述仍會誤觸每分鐘上限 | 使用者對同一張已描述的圖在一分鐘內點「AI 描述」7 次（每次都只是回既有筆）→ 第 7 次收到『描述得太頻繁』錯誤，體感為無故被擋。 | 先做 dup 檢查、確定要真正呼叫付費模型時才計入節流；或 dup 命中時退還該次計數。 |
| LOW | 知識庫＋版本＋回收桶（knowledge） | 還原回收桶知識繞過 sourceAssetId 去重，可產生同素材兩筆有效描述並被重複注入 LLM | 圖片描述後刪掉、再按一次 AI 描述得到新筆，接著到回收桶還原舊描述 → 知識庫同一張圖有兩份描述，AI 上下文重複餵入同段文字，浪費預算並可能誤導。 | restore 時若同 projectId+sourceAssetId 已有未刪有效筆（描述類再比標題前綴），阻擋或提示衝突。 |
| LOW | 知識庫＋版本＋回收桶（knowledge） | add 帶 sourceAssetId 時未排除已軟刪除素材，與 addFromAsset/describe 不一致 | 使用者手動貼文並引用一個已在回收桶的素材 id → knowledge.add 接受，建立指向已刪素材的知識（來源回溯失效，且與同功能其他入口行為不一致）。 | add 對 sourceAssetId 查詢加 isNull(deletedAt)，與其他入口統一。 |
| LOW | 知識庫＋版本＋回收桶（knowledge） | snapshot 與 update 非同一交易，update 失敗會留下多餘版本／並發雙寫產生重複快照 | 兩位組員近乎同時編輯同一筆逐字稿 → 版本歷史出現兩筆內容相同的快照，且先送出者的變更被後者無聲覆蓋。 | 將 snapshot+update 包入單一 db.transaction；並發覆寫可加樂觀鎖（updatedAt/版本號比對）。 |
| LOW | 知識庫＋版本＋回收桶（knowledge） | 編輯時 knowledge.get 失敗則 seededRef 永不為真，使用者無法儲存（含已存在的本地草稿） | 使用者上次改到一半留有本地草稿，這次點編輯但全文載入 API 恰好失敗 → 看得到自己的草稿卻永遠無法按儲存，也沒有重試按鈕。 | get 失敗時提供重試；或當已有本地草稿時允許在 seededRef 為 false 下以草稿送出（並提示未載入到最新全文的風險）。 |
| LOW | 角色定裝（characters） | characters.update 讀後寫的合併造成併發丟失更新（lost update） | A 改 name、B 改 appearance 幾乎同時送出：兩者都讀到舊 row，B 的寫入用舊 row.name 覆蓋掉 A 剛存的 name → A 的改名憑空消失。 | 改為只 set 有傳入的欄位（partial update，不回讀整列合併），或加 updatedAt 版本條件做樂觀鎖。 |
| LOW | 角色定裝（characters） | 每專案角色數量無上限，add 可無界建立 | 腳本或誤操作建立上千個角色 → list 全量回傳、前端一次渲染上千張卡片，載入與重繪變慢；DB 查詢無界。 | add 加每專案角色上限（例如 50/100）並回明確錯誤；list 視需要分頁或設硬上限。 |
| LOW | 角色定裝（characters） | 定裝錨點注入用完整 appearance（最長 6000 字），與知識庫注入的截短規則不一致 | 使用者把 appearance 寫滿 1000 字、勾 6 個角色 → 生成提示詞被塞入約 6000 字角色描述，壓過實際 prompt，影響生成品質。 | buildCharacterAnchor 對每個 appearance 也套用一致的截短上限，與知識庫注入規則對齊。 |
| LOW | 角色定裝（characters） | 既有卡的參考圖指向已進回收桶的素材時，選擇器顯示空但綁定仍在 | 綁了參考圖後把該素材丟回收桶 → 卡片縮圖消失、按「設參考圖」開啟 picker 也顯示空 → 使用者以為沒綁，但 DB 仍留著指向 trashed asset 的 referen | referenceUrl 為 null 但 referenceAssetId 非空時，在卡片明確標示「參考圖已在回收桶」，並提供一鍵清除綁定 |
| LOW | 分鏡（scenes：排序/就地生成/時長） | generateInto 的 prompt 覆寫無長度上限 | 直接以 generateInto 傳入超長 prompt（例如數十萬字）會被原樣送進 submitGenerationCore／注入世界觀後送 fal，可能造成模型端錯誤或異常計費 | 為 generateInto.prompt 加上與 addDraft 一致的 .max(2000)（或模型允許上限）。 |
| LOW | 分鏡（scenes：排序/就地生成/時長） | 同格就地生成／配音無後端併發去重，存在重複扣點視窗 | 同一格在兩個分頁或兩位協作者幾乎同時按『生成這一格』，兩筆生成都通過守門、各自扣點，該格最後只保留最後回填的成品，前一筆點數形同浪費。 | 送出前在交易內檢查該 sceneId+角色是否已有 queued/running 生成，若有則拒絕或回傳既有 generationId（冪等 |
| LOW | 分鏡（scenes：排序/就地生成/時長） | 時長非正數的防禦性下限各模組不一致 | 若任何非驗證路徑（migration、直接 SQL、未來新寫入口）讓 durationSec 變成 0 或負，粗剪預覽用 1 秒、交付時間碼用 3 秒、分鏡卡總長用原值，三處時間軸 | 抽一個共用的 sceneDur() 夾值函式（統一下限）供 player／exporter／SceneList 呼叫，SceneList 的 |
| LOW | 分鏡（scenes：排序/就地生成/時長） | addFromGeneration 找不到已入庫素材時靜默建出無素材分鏡 | 對一筆 status=done 但尚未落地素材（或文字類）的生成按『加入分鏡』，會產生一個 assetId 為 null 的空白分鏡格且不報錯，使用者以為加入成功卻看不到縮圖。 | 撈不到可用視覺素材時回 BAD_REQUEST（例如『此生成尚無可用素材』），比照 setVisualFromGeneration 的 !a |
| LOW | 場景設定（scenePresets） | buildSceneAnchor 不保序、不去重，且靜默丟棄不存在/跨專案的 presetId | 呼叫端（例如工作流或未來 API）傳入含他專案 id 或失效 id 的 scenePresetIds → 錨點靜默少一段光影設定，生成結果不含該場景設定但無任何提示。與 build | 至少對『請求筆數 > 命中筆數』的情況記錄或回報，避免使用者誤以為設定已生效；若需保序可依 presetIds 排序輸出。 |
| LOW | 場景設定（scenePresets） | 新增表單『場景參考圖』label 未與控制項關聯（a11y） | 螢幕閱讀器使用者移到參考圖控制項時讀不到對應標籤；點該 label 也不會聚焦到選擇器，與同表單其他欄位的無障礙行為不一致。 | 將此 label 與 ReferenceImagePicker 內的實際控制項以 id/aria-labelledby 關聯，或改用 fie |
| LOW | 提示詞庫（prompts） | navigator.clipboard 在非安全環境為 undefined，複製同步丟錯，'複製失敗' 回饋不顯示 | 以 http 或不支援的環境開啟時，點「複製」直接拋例外，既沒複製成功也不會閃「複製失敗」，使用者以為複製了但剪貼簿是空的。 | 先判斷 navigator.clipboard 是否存在並 try/catch，缺失時 fallback（execCommand 或提示手動 |
| LOW | 提示詞庫（prompts） | 提示詞文字（可達 2000 字）直接渲染無截斷/clamp | 存了很長的提示詞時，單列會撐出大量文字，把清單版面拉爆、右側按鈕被擠，清單可讀性變差。 | 加 CSS line-clamp（如 2-3 行）＋title/展開，保持列高一致。 |
| LOW | 提示詞庫（prompts） | 複製/再用/刪除按鈕缺 a11y 標記，複製結果無 aria-live 播報 | 螢幕閱讀器使用者無法得知每顆按鈕操作的是哪一則咒語，也聽不到「已複製/複製失敗」的結果回饋。 | 按鈕加 aria-label（含咒語摘要），複製狀態放入 aria-live=polite 區域播報。 |
| LOW | 自訂資料庫（databases：四層範圍/欄 | 資料庫清單硬上限 200 且無分頁/提示，超過者靜默不顯示（清單與 MCP 皆是） | 一個活躍團隊累積超過 200 個可見資料庫（或單庫超過 200 份文件）時，第 201 個起在工作台與 AI 介面完全消失，使用者無從察覺。 | 加入分頁或至少在達上限時回傳 hasMore 旗標並於 UI 提示；文件清單同理。 |
| LOW | 自訂資料庫（databases：四層範圍/欄 | 改欄位型別/選項後不重驗既有列，殘留值與新結構不一致且編輯時可能被靜默改寫 | 管理者把已有資料的文字欄改成數字欄，成員編輯任一舊列並存檔，原本的文字值被悄悄清成空；或移除 select 選項後舊列變成無法儲存。 | 改型別/選項時提示影響並考慮阻擋破壞性變更，或提供既有列重驗/遷移；至少在文案涵蓋型別與選項變更的後果。 |
| LOW | 自訂資料庫（databases：四層範圍/欄 | 文件全文預覽網頁端無法讀取超過前 20,000 字（MCP 有 offset 分頁、網頁沒有） | 上傳 10 萬字逐字稿，網頁使用者在預覽區永遠只看得到前 20,000 字，無法翻頁檢視其餘內容（AI 反而讀得到全文）。 | 在預覽區加入 offset 續讀（下一頁/載入更多），沿用既有 getFileText offset 參數。 |
| LOW | 自訂資料庫（databases：四層範圍/欄 | 前端 CSV 表頭以 naive split(',') 解析，含引號逗號的表頭與後端 RFC 解析不一致致欄位靜默漏對應 | 從 Excel 匯出、某欄名含逗號的 CSV，使用者自動對應後匯入，該欄資料全部遺失，結果只顯示「成功 N 列」不提示漏欄。 | 前端改用共用的 parseCsv 取第一列作為表頭候選，與後端同一套解析。 |
| LOW | 自訂資料庫（databases：四層範圍/欄 | 列更新為整列覆寫且無版本/衝突偵測，並行編輯造成後寫覆蓋（lost update） | 兩名成員同時雙擊編輯同一列，先後儲存，後儲存者的整列覆蓋先儲存者的所有欄位變更，無任何提示。 | 以 updatedAt 或版本號做樂觀鎖（If-Match 語意），衝突時回錯誤讓使用者重載；或改為只送變更欄位的 patch 合併。 |
| LOW | 資料庫文件匯入（databaseFiles： | 前端全文預覽只讀前 20000 字，無法載入後續 | 預覽一份 5 萬字逐字稿 → 只能看到前 2 萬字，其餘無法在介面檢視。 | 預覽加分頁/「載入更多」按鈕，用 getFileText 的 offset 續讀。 |
| LOW | 資料庫文件匯入（databaseFiles： | listFiles 查詢錯誤未在 UI 呈現 | listFiles 因網路/權限錯誤失敗 → 文件區一片空白、無錯誤也無載入中，使用者誤以為沒有文件。 | 加入 list.isLoading 骨架與 list.error 的 role=alert 提示。 |
| LOW | 資料庫文件匯入（databaseFiles： | 大檔（30MB–200MB）僅存檔時「僅存檔」文案誤導 | 上傳 40MB 的可抽字 PDF → 顯示「僅存檔／此格式暫不支援文字抽取」，使用者以為系統不支援 PDF。 | 依 sizeBytes 區分「格式不支援」與「檔案過大未抽字（上限 30MB）」兩種文案。 |
| LOW | 資料庫文件匯入（databaseFiles： | fetchNotionText 每個 block 重算 lines.join 長度，O(n²) | 匯入接近 800 blocks 上限的長 Notion 頁 → 每個 block 皆重複 join 全部已收集文字做長度判斷，抽取變慢。 | 維護一個累計長度計數器取代每次 join，或以 lines.length 近似判斷。 |
| LOW | 筆記排程＋.ics（notes/schedu | 排程「負責人」欄位半接線：清單有顯示 ownerName，但新增列與 MCP 都無設定 owner 的途徑 | 使用者想指定某筆行程的負責人 → 介面沒有任何欄位可設 → owner 顯示邏輯形同永遠走不到，是只做一半的功能（顯示側完成、寫入側缺）。 | 在新增/編輯排程加負責人下拉（同組成員），或若不做則移除 owner 讀取顯示以免誤導。 |
| LOW | 筆記排程＋.ics（notes/schedu | includePast 過濾用 startsAt>=now-24h，會誤藏「已開始但仍進行中」的跨日長行程 | 一個 3 天前開始、endsAt 在未來的長期活動（如拍攝週）→ 預設視圖把它藏起來（startsAt 已超過 24h 前），使用者以為活動不存在，需手動勾「顯示過去行程」才看得到 | 預設過濾條件改為 startsAt >= cutoff OR (endsAt is not null AND endsAt >= now)， |
| LOW | 筆記排程＋.ics（notes/schedu | 筆記編輯不重新解析 @提及，與新增路徑不一致 | 使用者新增筆記時忘了 @人，事後編輯內容加上「@小明 請跟進」→ 儲存後不會建立任何提及/通知，因為編輯路徑完全略過 mentions 解析，行為與新增時矛盾。 | 編輯時同樣 resolveMentions 並讓 notes.update 接受、驗證 mentions（或明確在文案說明編輯不觸發提及）。 |
| LOW | 筆記排程＋.ics（notes/schedu | schedule.update 路由無任何 UI 或 MCP 呼叫入口，且無法編輯 mentions/owner/project（近似死碼且 | 排程一旦建立就無法在 UI 修改（只能刪掉重建）；同時 update 端點是外露但無人使用的表面積，若日後接上又會因不處理 mentions 而與 add 行為不一致。 | 若產品要支援編輯排程，補 UI 並讓 update 與 add 對齊（含 mentions 重算）；若不支援，考慮移除或標記 update  |
| LOW | AI 導演建議（director） | mockSuggestions 的 kind 參數宣告但完全未使用（死參數／半成品個人化） | 不同 kind 的專案（如活動紀實）在 fallback／mock 路徑拿到與佛法禪修專案完全相同的三則罐頭建議；kind 明明被傳入卻不影響輸出。 | 若無意依 kind 分歧，移除該參數與所有呼叫端傳值；若要個人化，實作依 kind 切換建議樣板。 |
| LOW | AI 導演建議（director） | splitScriptCore 中 JSON.parse 丟例外走到泛用錯誤，訊息與 safeParse 分支不一致 | LLM 回傳被 max_tokens 截斷成 '[{"title":"甲","prompt":"...' → match 命中但 JSON.parse 丟錯 → 使用者看到「拆分鏡 | 將 JSON.parse 包成 try 或用一個小 helper 統一成 safeParse 失敗路徑，回傳一致的「AI 回傳無法解析，請再 |
| LOW | AI 導演建議（director） | ScriptSplitCard textarea 無關聯 label／aria-label（可及性） | 使用讀屏軟體的組員 Tab 進入拆分鏡輸入框，只聽到「編輯，空白」無法得知欄位用途。 | 為 textarea 補 aria-label="腳本內容"（或可見 label）；圖示狀態按鈕補 aria-label。 |
| LOW | AI 導演建議（director） | suggest 與 splitScript 共用同一 6 次/分鐘節流窗，且未在文件/文案說明合併上限 | 使用者用 AI 拆分鏡連拆 6 段後，立刻想按「給我 3 個分鏡 idea」被擋，卻沒有任何線索說明兩功能共用額度，體感像誤擋。 | 若為刻意設計，可在文案標明「AI 導演相關請求每分鐘上限 6 次（含建議與拆分鏡）」；否則拆成兩個獨立窗。 |
| LOW | AI 導演建議（director） | DirectorCard 切換專案時可能顯示前一專案的建議（mutation data 未依 projectId 重置） | A 專案發想出 3 則建議後切到 B 專案（同組件未重掛），DirectorCard 仍顯示 A 的建議；按「存成分鏡」會把 A 的提示詞存進 B 專案的分鏡。 | 以 useEffect 監看 projectId 變更時 suggest.reset() 並清 savedIdx/savingIdx；或在  |
| LOW | 專案助手（assistant） | 專案現況摘要漏計 awaiting_approval / rejected 兩種生成狀態 | 專案有 5 筆生成全部 awaiting_approval（待組長核准）→ 摘要顯示『完成 0／生成中 0／失敗 0』→ 助手回覆『目前還沒有任何生成』，與事實相反。 | 摘要補『待核 N／已駁回 N』，與 teamAssistant 對齊。 |
| LOW | 專案助手（assistant） | update_scene 標題長度上限(500)與 create_scene 標題(80)前後不一致 | 助手提議把某鏡標題改成 300 字→通過驗證寫入，產生一個 80 字新分鏡永遠無法達到的超長標題，破版且與新增分鏡規則不一致。 | update_scene 依 field 分別套用長度上限（title 80、voiceover 500）。 |
| LOW | 專案助手（assistant） | 多個提議動作可同時被確認執行，缺全域執行鎖 | 助手一次回覆提議 3 個 generate/run_workflow，使用者快速依序確認三顆→三個扣點動作並發送出，且 pendingKey 只記最後一顆，前一顆完成時因 k!== | 以全域 busy（run.isPending 或單一 inflight 旗標）停用所有動作按鈕，一次只允許執行一個提議動作。 |
| LOW | 專案助手（assistant） | teamAssistant 資料庫注入為逐表串行查詢(有界 N+1)，且超過 5 庫被靜默截斷無提示 | 組有 20 個資料庫→只注入最近更新的 5 個、其餘 15 個對助手隱形且無任何提示，組長問到未注入庫的內容時助手會答『查無資料』誤導；同時 5 表串行查詢在資料量大時拉長回應延遲 | 改用單次 inArray 批查所有 tableId 的 rows/files 再於 JS 分組；並在 context 附註被截斷的庫數。 |
| LOW | 專案助手（assistant） | 本週組花費以帳本淨額計，退款大於扣款時會顯示負數 | 上週生成大量、本週初大批失敗退款→context 出現『本週組花費 -120 點』這種不合理字樣餵給 LLM，助手可能照述負花費。 | 花費顯示夾為 Math.max(0, ...)，或扣款與退款分列。 |
| LOW | 專案助手（assistant） | 團隊彙總 AI 的回覆區塊缺 aria-live，螢幕報讀者聽不到非同步回覆 | 使用鍵盤/報讀器的組長按『詢問』→答案非同步塞進 <p>，報讀器完全靜音，使用者不知已有回覆。 | 答案容器加 role='status' 或 aria-live='polite'。 |
| LOW | 專案助手（assistant） | runAssistantAsk 的 resolve() 幻覺代號過濾與 runAction 各動作守門缺單元/整合測試 | 日後有人調整 resolve 或 runAction 的守門（例如漏了 continue、或 sceneId 歸屬檢查被移除）→ 迴歸不會被測試攔下，可能重現『把 assetId  | 補 resolve() 與 runAction 各動作分支的整合測試（含幻覺代號、跨專案 sceneId、不可綁鏡模型）。 |
| LOW | 團隊助手（teamAssistant） | 缺少單元／整合測試覆蓋（姊妹 router 有） | 日後有人改動聚合合併或空陣列守衛，沒有測試會攔到迴歸（例如漏了空 projectIds 守衛時對空組送出無效 SQL）。 | 補 teamAssistant.test.ts，覆蓋空組、超過 PROJECT_LIMIT、count 字串轉數、mock 模式輸出等。 |
| LOW | 團隊助手（teamAssistant） | 本週／每案花費可能顯示負數 | 某組近 7 天大量生成失敗自動退點、扣點很少 → 彙總文字顯示負的花費，讀起來荒謬也可能誤導 LLM 的資源分配分析。 | 以 GREATEST(-sum,0) 或 JS 端 Math.max(0, spent) 夾住下限，或明確標示「淨額（含退點）」。 |
| LOW | 團隊助手（teamAssistant） | 後端未 trim 訊息，前後端驗證不一致 | 直接打 tRPC 送 message="   " → 通過驗證、扣一次節流額度、對 LLM 送出空問題，得到無意義回覆。 | 用 z.string().trim().min(1).max(500) 或後端 message.trim() 後再判斷。 |
| LOW | 團隊助手（teamAssistant） | 每案『待審』計的是 approvals 列數而非分鏡數，語意混用 | 某分鏡被送審後駁回再送審、或存在專案級審批 → 待審數可能大於實際等待中的分鏡數，甚至超過 total，讓組長對待辦量產生錯誤印象。 | 以 count(distinct sceneId) 計、或把『待審』改標為「待審批件數」與分鏡分開陳述。 |
| LOW | 團隊助手（teamAssistant） | 自訂資料庫上下文為每表迴圈查詢（有界 N+1） | 組可見資料庫達上限時，單次 ask 多出約 10 次 DB 往返，且與聚合段的『無 N+1』設計原則相左。 | 用 inArray(tableId, visibleIds) 各一條 groupBy/窗函式查詢批次取回列與檔，或明列此為可接受取捨。 |
| LOW | 回饋系統＋回饋代理（feedback/fee | 回報者無信箱時 emailStatus 落地為 "failed"，每輪補寄都重撈後 continue，語意錯置且無限重試 | 以無 email 的邀請帳號送出回饋 → agent 分診 → emailStatus="failed"；此後每 3 天巡檢都重複撈取此列、continue，狀態永遠 failed | 為「無收件者」新增獨立狀態（或沿用 skipped），resendFailedReplies 只撈真正暫時性失敗的列。 |
| LOW | 回饋系統＋回饋代理（feedback/fee | 截圖仍在擷取或上傳失敗時，勾了要截圖仍會靜默送出無圖，且送出鈕不因擷取中而停用 | 使用者填完就快速按送出，此時截圖還在擷取（capturing=true）→ 回饋送出但沒有截圖，使用者以為附上了。 | 擷取進行中時 disable 送出鈕或送出前 await 擷取；上傳失敗時給一行提示「截圖未能附上，文字仍已送出」。 |
| LOW | 回饋系統＋回饋代理（feedback/fee | /my-reports 頁未納入 pageForPath 與 FEEDBACK_PAGES，在該頁回報無法帶入/勾選當前頁 | 使用者在 /my-reports 頁對某元件送回饋，頁面複選全空、無「・目前」標示，審閱端無從得知回報發生在哪一頁。 | 在 pageForPath 加上 /my-reports 對應，並同步加入 FEEDBACK_PAGES。 |
| LOW | 回饋系統＋回饋代理（feedback/fee | LLM 未給 reply 時，agentReply 落地為含署名的整封信文，站內回覆卡會顯示 email 頁尾 | 測試模式或 LLM 回空 reply 時，回報者在「我的回報」看到回覆內含「此信由回饋代理自動寄出」的 email 署名。 | 站內顯示與 email 內文分離：agentReply 只存純回覆（reply 或分類保底回覆），email 署名只在 buildReply |
| LOW | 回饋系統＋回饋代理（feedback/fee | 審閱者改狀態不會反映到回報者已開啟的追蹤頁；追蹤頁無輪詢，代理回覆需手動重整才出現 | 回報者送出後停在追蹤頁等待；代理即時分診完成並回覆，但頁面不更新，使用者以為未被處理。 | MyReportsPage 加上合理的 refetchInterval 或在視窗聚焦時 refetch，讓文案承諾的『狀態會更新』成立。 |
| LOW | 回饋系統＋回饋代理（feedback/fee | 回饋代理核心副作用（寄信/補寄/guard/skipped 判斷/併發）完全無測試覆蓋 | 未來調整補寄條件或狀態機時，skipped 漏補、重複寄信等回歸無法被測試攔下。 | 以可注入的 db/sendEmail mock 補上 reviewAndReplyOne 與 resendFailedReplies 的行為 |
| LOW | 模型目錄與指南（models/catalog | MCP find_model 把 workflow 當可篩類別廣告給代理，且描述硬寫「11 類」易與 CATEGORIES 漂移 | 外部代理讀 find_model 說明，選 category='工作流/workflow' 想找工作流模型 → 回空陣列且無解釋，代理誤以為系統沒有工作流。 | find_model 列類別時 filter 掉 workflow，並把「11 類」改為由 `CATEGORIES.filter(c=>c. |
| LOW | 模型目錄與指南（models/catalog | ModelPicker 切換類別期間無 placeholderData/空狀態，短暫對父層送出 onChange(null) | 使用者在生成面板快速切換創作類別 → 送出鈕/點數估算在每次切換閃一下（null→有值）；體感為畫面抖動。 | byCategory 加 `placeholderData: (prev)=>prev`（比照 ModelsPage 的 search），並 |
| LOW | 模型目錄與指南（models/catalog | model_catalog 表除啟動自檢計數外無程式讀取，屬近乎 write-only 的目錄 | 維護者改動 syncCatalog 欄位或誤以為站內 find_model 讀此表 → 改錯地方；或外部代理查 model_catalog.points 看到 TTS 固定點數，實 | 若確實要支援外部 SQL 查詢，於文件標明 points 為基準值並補充動態計費說明；若無實際讀取者，考慮移除該表與同步以免維護負擔與誤導。 |
| LOW | MCP 介面與金鑰（mcp/mcpToken | 建立金鑰上限檢查存在 TOCTOU 競態，可超過 MCP_TOKEN_MAX_PER_USER | 使用者（或腳本）在極短時間內連發多個 create 請求；多筆同時讀到 count=9 → 全部通過 → 有效金鑰數變成 11+ 把，突破「擋無限灌爆」的設計意圖。 | 在單一交易內鎖定該 user 的 token 列後再計數插入，或以資料庫層條件約束（例如插入前 SELECT ... FOR UPDATE  |
| LOW | MCP 介面與金鑰（mcp/mcpToken | isMcpEnabled 只看 revokedAt，忽略 expiresAt：只剩過期金鑰時仍回報「已啟用」 | 使用者建了一把 7 天金鑰、到期後未撤銷，且無其他金鑰／無 env 金鑰。handleMcp 不回「MCP 未啟用（請建立金鑰）」的 404 指引，改回 401「金鑰不正確或已撤銷 | isMcpEnabled 的查詢加上「expiresAt IS NULL OR expiresAt > now()」條件，與 resolve |
| LOW | MCP 介面與金鑰（mcp/mcpToken | REST 新增列失敗不落審計，且 REST 操作不出現在「近期 MCP 活動」 | 外洩金鑰被拿去打 REST POST /rows：成功的寫入不顯示在 MCP 活動頁、失敗的寫入完全無紀錄，使用者無從察覺異常。 | REST 寫入的成功與失敗都落審計；recentActivity 改以 like 'mcp.%' OR 'rest.%'（或另立區塊）納入經 |
| LOW | MCP 介面與金鑰（mcp/mcpToken | CSV 匯出在 50,000 列硬上限靜默截斷，未告知呼叫端 | 某資料庫有 6 萬列，使用者點「匯出 CSV」拿到 5 萬列的檔案，畫面與檔案都無任何截斷提示，據此做的統計全部漏掉 1 萬列。 | 超過上限時於回應標頭或檔案結尾標註被截斷並提示改用 REST 分頁，或提供分頁/串流匯出。 |
| LOW | MCP 介面與金鑰（mcp/mcpToken | ?key= 查詢字串認證開放於所有 REST/CSV 端點，金鑰易外洩到日誌與瀏覽紀錄 | 使用者複製一條含 ?key= 的 /api/v1/databases/…/rows 網址貼給同事或存進書籤，長效個人金鑰即隨網址外流；即使唯讀限制仍在，金鑰本體已洩漏。 | ?key= 僅限確實無法帶標頭的訂閱型端點（ICS）；REST v1／CSV 只接受 x-api-key 標頭，或對查詢字串金鑰限制為唯讀  |
| NIT | 登入/註冊/邀請/改密碼/重設（auth） | ChangePasswordDialog 錯誤未經 friendlyAuthError，與 LoginPage/AcceptInvitePa | 後端 changePassword 若因某欄回傳未被 errorFormatter 收斂的 zod JSON → 使用者在改密碼框看到原始 JSON。 | 統一走 friendlyAuthError(change.error.message)。 |
| NIT | 登入/註冊/邀請/改密碼/重設（auth） | 前端 email 驗證正則與後端 zod .email() 規則不一致 | 使用者輸入前端正則通過但 zod 判為 invalid 的 email → 本地不擋、送出後才吃後端錯誤，多一次往返。 | 統一驗證來源（前端沿用與 zod 相近的規則，或索性只做「非空+含@」的輕量預檢並以後端為準）。 |
| NIT | 工作流串鏈（workflows） | me 查詢載入前 hasMyActive 為 false，自己在跑的 run 短暫不鎖執行按鈕 | 頁面剛載入、me 尚未回來時，使用者已有一條 run 在跑，此刻填了想法按執行，前端未攔、送出後由伺服器回『你已有一條工作流在跑』。 | me 尚未載入時可先停用執行按鈕，或以 runs 內任一屬於自己的活躍 run 判定並在 me pending 時保守鎖定。 |
| NIT | 生成台（generation：送出/輪詢/退 | characterIds/scenePresetIds 未去重 | 前端 bug 或直呼傳入同一角色 id 兩次→錨點文字被重複串接。 | zod 層對陣列去重或加唯一性驗證。 |
| NIT | 生成台（generation：送出/輪詢/退 | 多列同時完成觸發多次 StatusPoller invalidate | 工作流一次跑完 8 鏡→短時間內 8×5 次 invalidate 重觸發，多支查詢連續重取。 | 由列表層集中偵測完成邊緣後統一 invalidate（已有 prevStatusRef 可複用），移除每列 poller 的重複 inval |
| NIT | 審批三態機（approvals：送審/通過/ | 重送作廢舊 pending 時未寫 decidedBy，歷史列看似「被退回卻無裁決人」 | 審批歷史（若日後呈現）中，因重送而作廢的舊列狀態為 needs_work、有 reason 卻無 decidedBy，和真正被人退回的列在資料上不可區分，且缺裁決人欄，語意不一致。 | 重送作廢與裁決作廢的欄位語意對齊，或為「系統作廢」引入獨立狀態/標記以區分人為退回。 |
| NIT | 站內留言＋@助手＋即時同步（messages | @助手 空問題仍呼叫 NIM | 使用者只打「@助手」送出 → 空問題仍打一次 NIM。 | cleanQ 為空時回一句「想問什麼？@助手 後面接上問題」而不呼叫模型。 |
| NIT | 站內留言＋@助手＋即時同步（messages | 表情選單與引用 picker 無外點關閉／Esc 關閉 | 打開表情選單後點頁面他處 → 選單不關，持續浮在留言上。 | 加外點/Esc 關閉與適當 aria role。 |
| NIT | 站內留言＋@助手＋即時同步（messages | 語音錄音無最長時長上限 | 使用者按錄音後長時間未停 → 累積巨大 blob，停止時才在上傳階段失敗。 | 設定最長錄音秒數，達上限自動停止並提示。 |
| NIT | 知識庫＋版本＋回收桶（knowledge） | RecycleBin 錯誤聚合取第一個非空，成功後仍殘留他項舊錯誤 | 還原素材 A 失敗留下錯誤，接著成功還原知識 B → 頂部仍顯示 A 的『操作失敗』訊息，使用者誤以為 B 也失敗。 | 錯誤與具體列綁定顯示，或在任一操作開始/成功時 reset 其他 mutation 的錯誤狀態。 |
| NIT | 角色定裝（characters） | 角色名稱允許重複，錨點注入時無法區分 | 建立兩個都叫「安倢」的角色 → 生成勾選時兩張卡標題相同無法分辨；注入提示詞也是兩段同名描述，AI 導演難以對應。 | add/update 檢查同專案 name 唯一（或至少前端警示重名）。 |
| NIT | 分鏡（scenes：排序/就地生成/時長） | move 為共用 mutation，pending 期間停用所有分鏡的↑↓ | 使用者連續調整多格順序時，每按一次都要等上一筆 move 完成才能再操作任一格，體感卡頓；非正確性錯誤但影響排序效率。 | 可接受此設計以避免併發交換；若要改善，改為僅停用參與交換的兩格，或以樂觀更新處理排序。 |
| NIT | 場景設定（scenePresets） | lighting 空白字串與 null 兩種『無光線』表示不一致 | 純空白光線輸入會落地成 '' 而非 null。目前 buildSceneAnchor 與 knowledge 都以 falsy 判斷（s.lighting?）故不影響輸出，但資料層 | trim 後為空時統一存 null（例如 input.lighting?.trim() \|\| null），與『無光線』單一表示對齊。 |
| NIT | 提示詞庫（prompts） | 去重＋useCount 併發（advisory lock）與 2000 字上限等關鍵邏輯無單元測試 | 未來若有人動到去重或鎖邏輯（例如誤加唯一索引、改 hashtext 參數、拿掉交易），迴歸不會被測試攔下。 | 補單元/整合測試覆蓋：同專案同文字二次 save→useCount 遞增且僅一列、併發 save 不重複、空白 trim 後 no-op。 |
| NIT | 自訂資料庫（databases：四層範圍/欄 | 20,000 列上限為先 count 後 insert，併發寫入存在越界競態 | 接近上限時多個 REST/MCP 客戶端同時寫入，實際列數可略微超過 20,000（保險絲失準）。 | 以資料庫端條件插入或約束/交易序列化該檢查；此為軟上限，影響輕微但可強化。 |
| NIT | 自訂資料庫（databases：四層範圍/欄 | CellDisplay 對 url 欄放行 mailto: 為可點連結，但 url 欄寫入驗證只允許 http(s)，該分支為死碼 | 無實際危害，屬不可達分支/誤導性白名單；若未來想支援 mailto，需同步放寬寫入驗證。 | 移除 mailto 分支，或在 validateRowData 一併允許 mailto 以對齊顯示端。 |
| NIT | 資料庫文件匯入（databaseFiles： | 接受 0 byte 空檔上傳，無驗證 | 誤選空檔或上傳中斷產生 0 byte → 建立一筆無內容的文件列，需手動刪除。 | req.file.size===0 時回 400 拒收。 |
| NIT | 資料庫文件匯入（databaseFiles： | refreshFile 二進位 sizeBytes 用 buf.length 而非 saved.sizeBytes，與 importUrl  | 未來 saveBuffer 改為壓縮儲存 → importUrl 與 refreshFile 對同一檔記錄不同 sizeBytes，配額與顯示不一致。 | refreshFile 也採用 saved.sizeBytes。 |
| NIT | 資料庫文件匯入（databaseFiles： | sizeBytes 為 int32 欄位，ASSET_MAX_MB 調高時單檔可能溢位 | 營運者為支援大型影音把 ASSET_MAX_MB 設 4096 → 上傳 3GB 檔時 sizeBytes 欄位溢位。 | sizeBytes 改 bigint，或在上傳處對 int32 上限做保護性檢查。 |
| NIT | 筆記排程＋.ics（notes/schedu | 排程/筆記的刪除鍵用單一 isPending 旗標，刪一筆會停用整份清單所有刪除鍵 | 同時想刪多筆 → 點下第一筆刪除的短暫 pending 期間，畫面上所有其他刪除鍵一起變灰，看起來像卡住，需等前一筆完成才能點下一筆。 | 以「正在刪除的 id」判斷個別按鈕 disabled，而非整卡共用 isPending。 |
| NIT | 專案助手（assistant） | messageAssistant 只移除第一個 @助手，多次觸發字串殘留於送 LLM 的問題 | 使用者留言『@助手 幫我看 @助手 這段』→ cleanQ 變『幫我看 @助手 這段』，多餘 @助手 進提示詞，輕微干擾。 | 改 replaceAll 或全域正則。 |
| NIT | 專案助手（assistant） | STATUS_LABEL 含永不出現的 review 鍵（死碼） | 無功能影響，僅為誤導後續維護者的死映射。 | 移除 review 鍵或補註為預留。 |
| NIT | 團隊助手（teamAssistant） | 三個助手 router 各自獨立的節流桶，單人可疊加超量呼叫免費 LLM | 同一使用者同時開多個助手狂送 → 合計超過任一單桶意圖的呼叫量，較易撞到 NIM 429（雖已被優雅處理）。 | 若意圖是全站每人 LLM 呼叫上限，改用共用的 per-user 節流；否則保留現狀但註明是刻意的每功能獨立配額。 |
| NIT | 回饋系統＋回饋代理（feedback/fee | 六題滿意度：既有回饋若取消掉所有分數則因 rated===0 而無法送出，等於無法清空重填 | 已填過的使用者想大改，先逐題取消再重評，但一旦全部取消就卡在無法送出。 | 允許 rated===0 時仍可送出（至少對 hasExisting 者），或提示「至少評 1 題」時保留可清空路徑。 |
| NIT | MCP 介面與金鑰（mcp/mcpToken | env 共用金鑰解析超級管理員未指定排序，多位管理員時身分不確定 | 部署有兩位開發者帳號、仍在用 MCP_API_KEY 相容金鑰；不同請求可能被記到不同開發者名下，事後追審計時歸屬對不上。 | 加上穩定排序（如 orderBy createdAt asc）或改綁明確的系統帳號，讓 env 金鑰身分具決定性。 |